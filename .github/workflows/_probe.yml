name: _PROBE reusable call test

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  single:
    name: call trivial callee
    uses: ./.github/workflows/_callee.yml
    secrets: inherit
