# 2026-09-03 自动化摘要

## AIShield 生态雷达每日闭环（02:00 / automation-1788100888350）

**结果行：`[done] new=9 drafted=2 issues=0 errors=4` —— 数值正常，无新攻击类别，常规日，不单独推送聊天告警，并入周日 20:00 合并周报。**

| 项 | 值 | 判读 |
|----|----|------|
| new | 9 | 偏低但**可解释**（见下"new 骤降归因"）：同一 UTC 日内 10h 前刚跑过一次，7 天窗口已被去重 |
| drafted | 2 | 均为**既有类别**（prompt-injection / skill-poisoning）→ 不触发即时提醒 |
| issues | 0 | dry-run，未建 GitHub issue（符合预期） |
| errors | 4 | **低于 >8 告警阈值**；连续第 3 日同源同因 |

### 源失败说明（errors=4）
- 6 类源扫描行全部 OK：github-trending 39 / arxiv 40 / hn 7 / reddit 4 / standards 0 / user-platform 6（共 96 条入口条目）。
- 4 个 error 全为 Reddit 4 个子版 fetch `HTTP -1`（LocalLLaMA / MCP / ClaudeAI / Anthropic），与 09-01、09-02 **完全一致**，属本机网络出口对 Reddit 不可达，**非脚本缺陷**；Reddit 源整体仍产出 4 条。不告警。
- standards（OWASP·LF）连续多日 0 items —— 非报错，是该源本身低频；已是既有基线，无需处置。

### 起草候选分类（drafted=2）
| 文件 | 类别 | 是否新威胁向量 | 依据 |
|------|------|----------------|------|
| PROPOSED_20260902_alphaparkinc_genpark_prompt_in_ca33e6 | prompt-injection | 否（既有，第 14 例） | `_proposed/` 自 2026-08-10 起累计 14 条同类别 |
| PROPOSED_20260902_alphaparkinc_genpark_distribut_31fc1a | skill-poisoning | 否（既有，第 2 例） | 首例 2026-08-25 已在跟踪 |

→ **无新攻击类别，按约定不即时提醒用户 review。**

### ⚠️ 质量观察（建议周报处置，勿在日报动代码）
1. **两条候选都是防御侧误标，且同一账号刷仓。** 源均为 `alphaparkinc/genpark-*-skill`：
   - `genpark-prompt-injection-jailbreak-firewall-sentinel-skill` = **防火墙/哨兵防御工具**，非攻击样本；
   - `genpark-distributed-agent-chaos-latency-fuzzer-skill` = **混沌/延迟压测器**，被归为 `skill-poisoning` 属**明显误分类**。
   同一 owner 连出两条高严重度候选，疑似 `genpark-*` 批量刷仓（repo-spam farm）。
   与 09-01 记录的"防御侧论文产死稿"（ROPE / Circuit Discovery）**同一根因**：起草链路只做关键词命中，不区分攻击侧/防御侧。
   → 周报评估项（已第 3 次复现）：加**攻防侧分流过滤** + owner 级刷仓抑制。
2. **`_proposed/` backlog 继续腐烂**：现 49 个 PROPOSED_*.json，**status 全部 `draft`、零 `ready`、零晋升**，regex 均为 `TODO`。
   类别分布：prompt-injection 14 / jailbreak 13 / n/a 6 / memory-poisoning 5 / trajectory-poisoning 4 / adversarial-agent 2 / mcp-attack 2 / skill-poisoning 2 / backdoor 1。
   → 周报处置建议：按类别合并为少量带**真实 regex** 的 `ready` 候选走 `promote_rule.py --promote`，无价值的直接删；否则起草链路只在产垃圾。

### 📌 new 骤降归因（9 vs 昨日 57，非故障）
- 上一次运行时间 `2026-09-02T07:41Z`，本次 `2026-09-02T18:03Z` —— 间隔仅 ~10.4h，`--days 7` 窗口高度重叠，96 条入口条目中 90% 命中去重（seen_ids 905 → 971）。
- **副作用：报告文件被覆盖。** 两次运行落在同一 UTC 日 → 都写 `docs/intel/2026-09-02-tech-radar.md`，昨日 57 条信号的报告正文**已被本次覆盖**（摘要仅存于 `2026-09-02.md` digest）。
  `docs/intel/` 现存 08-28 / 08-29 / 09-01 / 09-02，缺 08-30 / 08-31 疑同因。
  → 周报评估项：报告文件名加时间戳或运行序号，避免同日覆盖（**日报不改代码**）。

### 备注
- `data/state/tech_radar.json`：runs=36（基线 34，+2）、seen_ids=971（基线 905）、last_run=2026-09-02T18:03:29Z、arxiv_endpoint=listing（三级 fallback 未降级）。
- 报告产出：`docs/intel/2026-09-02-tech-radar.md`。
- 脚本 exit 0，单次执行未重跑；未推送远端、未改脚本代码。

---

## 追加执行：`_proposed/` 队列分诊（同日，非雷达脚本范围）

**已把上面「周报评估项 2」当场做掉：队列 49 → 13，并产出史上第一条 `ready` 候选。**

| 动作 | 结果 |
|------|------|
| 新增 `scripts/triage_proposed.py` | 带 `--plan` / `--apply` / `--restore` 三态，处置**可逆** |
| 全 49 条显式分诊 | reject 36 / keep 13 / unclassified **0** |
| 36 条移入 `scanner/_proposed/rejected/` | **未删除**，每条写回 `status=rejected` + `reject_reason` + 中文理由 |
| 产出 `rejected/REJECTED-INDEX.md` | 理由码表 + 36 条逐条说明 + 13 条保留理由 |
| 撰写 SkillBloat 真 regex（2 条 pattern） | `promote_rule.py --check` → **ready:1 blocked:0**，队列首次出现可晋升项 |
| 规则数 | 仍 **228 MCP / 234 Skill**，未晋升、未改线上 |

### 分诊理由码分布（36 条被拒）
| 码 | 数量 | 含义 |
|----|------|------|
| `R1-defense-side` | 13 | 防御侧工具/论文或 `genpark-*` 刷仓，无攻击面 |
| `R3-off-scope` | 7 | 与 MCP/skill/prompt 扫描无关（智能合约、移动端、JS fuzzing 等） |
| `R2-benchmark` | 5 | 评测方法学，度量攻击而非攻击 |
| `R4-capability` | 4 | 自进化 agent **能力**论文被误标 trajectory-poisoning |
| `R6-covered` | 4 | 记忆投毒类，已被现网 4 条规则覆盖，晋升只增重复 |
| `R5-out-of-reach` | 3 | 权重级后门 / 多模态 / 文生图，文本扫描器天然不可见 |

**关键判断（推翻此前的处置倾向）**：此前建议「把 5 条 memory-poisoning 合并为 1 条 ready」。实测现网已有 4 条覆盖该面
（雷达 `轨迹投毒`、雷达 `持久载体`、静态 `memory.upsert/insert 写入`、静态 `共享/持久记忆`）→ **合并晋升会产出重复规则**，
故 4 条全部按 `R6-covered` 归档，不再作为待办。真正的覆盖空缺在别处：**Token 放大**。

### 唯一 ready 候选：SkillBloat（skill-poisoning / high）
- 依据：现网 Token 相关规则只有「硬编码 Token」「Token 上限缺失」「大上下文窗口」三类**配置项**，无任何**放大指令**特征。
- 两条 pattern：① 三位数以上次数的输出重复（`repeat/re-output/duplicate/重复/复述 … \d{3,} times|次|遍`）；② 指令生成直至上下文/Token 预算耗尽。
- 验伪：4/4 攻击样本命中；10 条 benign 语料 + 6 条对抗性负样本（`Repeat this step 5 times`、`at least 2000 words`、`Retry up to 3 times`、`重复执行该命令两次`）**零误报**。三位数下限是把合法小次数重复与放大攻击分开的关键。
- **默认未晋升、未推送**（无人值守下的保守默认）：`status=ready` 是设计上的交接点——推送后 `closed-loop-spine.yml` 会自动过门禁晋升并部署。就地本地晋升反而会绕过该编排，且会改写 npm 包首页 `mcp-server/README.md` 的规则数。
- 用户一条命令即可放行：`python scripts/promote_rule.py --promote PROPOSED_20260825_skillbloat_token_amplification_6aac90.json`

### 剩余 12 条待撰写（按优先级）
1. `forbiddengarden_poisonvine`、`anonymous_beta_kaiju`（**mcp-attack，最对口，需读仓库确认载荷**）
2. `jailbreakskill`（以 skill 为载体的可复用越狱，正对 skill 扫描面）
3. `from_prompt_injection_to_web_e`（critical，需先核对是否已被外传/端点规则覆盖）
4. 4 条越狱合集（`ai-jailbreak-prompts` / `jailbreak-kit` / `Universal-AI-Jailbreak-Collection` / `All-AI-Jailbreaks`×2，其中两条 All-AI-Jailbreaks 疑同源可合并）
5. `psychjail`、`toward_metacognitive_one_shot`、`soulkyu_leandro`（后者标题无信息，需读仓库定去留）

---

## 跟进：起草链路根因已修复（同日续，已落 main）
- 上面「周报评估项：加攻防侧分流 + owner 级刷仓抑制」**已当场做掉**：`tech_radar.py` 的
  `classify_signal` 现在返回 `(cat, sev, side)`，起草/issue 门禁仅对 `side=="attack"` 触发；
  `genpark-*` 等同 owner 刷仓源被 `_is_repo_spam` 直接抑制为 spam。防御侧论文/工具不再起草攻击候选
  （报告内标注 `_(defense-side, not drafted)_`），从根上消除 49 腐烂草稿的成因。
- 测试：新增 2 条回归用例，`tests/test_tech_radar.py` 39/0 全绿；已推送 main（c0cbbf0d，API 复验通过）。
- 注意：此修复**不会改变已存在的 49 条 `_proposed/` 文件**——它们仍需按此前分诊结果（36 reject / 13 keep）处置；
  SkillBloat 仍 `ready` 待用户放行晋升。

---

## AIShield 每日安全守夜（08:30 / automation-1786840209961）

**结果行：`[PASS] 对外发布物自检=PASS | 线上存活=PASS | 测试门禁=PASS` —— 三项全绿，无关键异常，常规日，不单独推送聊天告警，并入周日 20:00 合并周报。**

| 检查项 | 结果 | 关键异常 |
|--------|------|----------|
| 对外发布物自检（distribution gate） | PASS | 无 |
| 线上存活（health.json + aishield.tools） | PASS | 无 |
| 测试门禁（592/0/12 + 核心契约） | PASS | 无 |

### 明细
- **distribution gate**：`ok=true`，`total=5`，`passed=5`，`failed=0`，`blocking=0`；不变式 `no_execute/no_network=true`。5 渠道评分 94–97（agensi 97 / claude-skills 96 / gpt-store 95 / huggingface 94 / clawhub 94）。非阻断提示：agensi 线上 1.0.0 落后仓库源 1.1.0。
- **线上存活**：远端 health.json `updated=2026-09-02T21:08:59Z`（≈运行前 3.3h，新鲜）；aishield.tools HTTP 200（28,509B）。
- **测试**：592 通过 / 0 失败 / 12 跳过（基线 523/0/9）；核心契约（良性零误报 + 恶意仍拦）通过；27/27 模块全绿。
- 报告产出：`eco/reports/guard-2026-09-03.md`，将经 `scripts/gh_push.py` 推 main 并 Contents API 复验。
