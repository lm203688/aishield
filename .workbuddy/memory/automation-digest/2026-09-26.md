# 自动化执行摘要 — 2026-09-26

## 🛰️ AIShield Tech Radar（每日闭环）

- **命令**：`scripts/tech_radar.py --sources all --days 7 --publish`
- **结果**：`[done] new=70 drafted=0 issues=0 errors=4`，脚本 exit 0，单次执行未重跑。
- **源健康**：`[health] known-blocked sources (no action): reddit(12)` —— reddit 连续 12 次失败但属 known-blocked（本机出口结构性不可达），**无 `[health] DEGRADED sources:` 行** → 不构成「连续 2 天 DEGRADED 同一源」→ 不触发 🟡 降级升级。
- **errors=4** 同源同因（Reddit 4 子版 `HTTP -1`：LocalLLaMA / MCP / ClaudeAI / Anthropic），网络出口结构性不可达，非脚本缺陷，远低于 >8 阈值 → 不告警。
- **drafted=0**：本日无规则候选产出，攻防侧分流生效（arXiv 攻击侧新条目如 trace-tampering / output-prefix / collusion / denial-of-wallet 等多标 defense-side 或未达起草阈值）→ 无新威胁向量 → 常规仅入 digest，无即时提醒、无跨项目升级。
- **new=70**：距上次记录（09-23，runs=55 / seen_ids=1627）约 3 天；期间 runs=55→**58**（+3，含 09-24/09-25 疑似静默运行），seen_ids=1627→**1738**（+111），`--days 7` 窗口高度重叠后仍有较多新条目，去重自然偏高，非故障。
- **`--publish` 全 OK**（无复发 sha 故障）：report→`2a91b4d8` / index.md→`15461d37` / state→`81bb923b`。远端 state 已刷新，M8 元监控可读到最新源健康。
- 状态：`data/state/tech_radar.json` runs **58**、seen_ids **1738**、last_run `2026-09-26T00:35:09Z`。
- **升级判定**：三项条件（新攻击类别 / errors>8 连续 2 天 / DEGRADED 连续 2 天）均不触发 → 仅写本地 digest，未写 OPEN_ISSUES.md、未发独立提醒、未改代码、未建 issue、未手动 promote。

### 备注（待周报评估）
- Reddit 源自 09-12 起连续 known-blocked（今 12 次），长期结构性不可达；可考虑从 radar 源清单移除或降级为「可选」以减少噪音（待周报与用户拍板）。
- 09-24 / 09-25 两次运行未在本 automation 记忆中留痕（runs +3 推断其已静默执行），下次若再观察到「runs 跳增但无 digest」，需排查 automation 是否漏记。

---

## 🛡️ AIShield 每日安全守夜（08:30 CST）

- **① 对外发布物自检**（`scripts/self_scan.py --json`）：**PASS**。ok=true；totals: findings=54 / suppressed=27 / **blocking_unsuppressed=0**；stale_allowlist_entries=[]（允许清单无失效）；invariants no_execute=true / no_network=true（隔离不变量守住）。11 源阻断级发现全空，27 条抑制均为自指误报按 (source,file,type) 精确消噪；无真实高危、无需新增 allowlist 条目。
- **② 线上存活**（curl `--ssl-no-revoke --tlsv1.3`）：**PASS**。远端 health.json（Contents API, ref=main）`updated=2026-09-25T21:41:29Z`（age≈2h55m，FRESH）、healthy 4/4 / consecutive_failures=0；aishield.tools 首页 HTTP 200 / 28818B；/api/v1/health v4.3.0 / 235 规则 / commit `e746884a` / deployed_at 2026-09-23T01:56:42Z；main HEAD `81bb923b`（"chore(radar): publish 2026-09-26 tech radar"）。**部署漂移（信息性，非升级）**：线上 commit 落后 main HEAD 若干提交、deployed_at 约 3 天旧，站点与 health 均存活非失活，属 Spine(11:17 CST) 部署前漂移，不满足「连续 2 天失活」升级条件。
- **③ 本地测试**（`tests/run_all.py`）：**PASS**。Ran 1579 tests / 0 failure / 0 error / 23 skipped → **ALL TESTS PASSED**（~79.7s）；核心契约（良性零误报 + 恶意仍拦）成立。日志 `FAIL: recall 0.9000 < 1.5000` 为 benchmark.py 故意召回门禁探针，非真实失败。
- **关键异常**：无（测试未转红 / 无未登记阻断级发现 / 线上未失活）。**不触发跨项目升级**，仅写本地 digest。
- 产出：`eco/reports/guard-2026-09-26.md`。
