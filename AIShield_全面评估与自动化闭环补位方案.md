# AIShield 全面评估与自动化闭环补位方案

> 评估日期：2026-08-04
> 评估范围：`C:\Users\xing\Desktop\aishield` 全目录 + GitHub Actions + WorkBuddy 调度器 + 线上服务实测
> 评估基准：《AIShield 生态体系开发总纲 v1.0》三大原则（自动化 / 生态化 / 盈利化）
> 结论提要：**项目具备正确的赛道判断和完整的架构设计，但自动化体系存在 5 处结构性断裂，当前不具备"无人值守运营"的前提条件。**

---

## 第一部分：扫描概览 — 项目真实状态快照

### 1.1 代码资产盘点

| 层 | 模块 | 规模 | 生产就绪度 |
|----|------|------|-----------|
| 安全层 | `scanner/`（engine/rules/handshake/rug_pull/monitor/llm_analyzer/batch_scanner/api_scanner） | 9 个模块，约 16 万字符 | ✅ 可用，133 条规则 |
| 生态层 | `eco/`（a2a_gateway/dispatcher/observability/payment/badge/marketplace/skill_market/sandbox/identity/collab/account/auth_provider） | 15 个模块，约 40 万字符 | ⚠️ 多数内存存储，非生产级 |
| 协议层 | `mcp-server/`（TypeScript） | 2512 文件 | ⚠️ 代码完成，**未发布 npm** |
| API 层 | `api/`（Flask 8420 + FastAPI 8450） | 双服务并存 | ⚠️ 技术债，总纲要求合并 |
| 测试 | `tests/`（run_all/e2e/security/geo/linkages/quick） | 6 个测试文件 | ✅ 存在且被 CI 引用 |
| 自动化 | `.github/workflows/` | 14 个 workflow | ❌ **2 个致命缺陷，详见第二部分** |

### 1.2 业务硬指标（截至 2026-08-04）

| 指标 | 实际值 | 总纲 Phase 1 月末目标 | 达成率 |
|------|--------|---------------------|--------|
| 月收入 | **¥0**（2,500 积分为测试充值） | ≥ ¥500 | **0%** |
| 注册用户 | **1**（测试账户 IdempTest） | ≥ 50 | **2%** |
| Agent 日调用量 | **0**（近 9 天无 usage 记录） | ≥ 50 | **0%** |
| 扫描数据库工具数 | **21** | 100 | **21%** |
| MCP Registry 上架 | **0 / 7** | 7 | **0%** |
| npm 周下载量 | **未发布** | 50 | **0%** |
| GitHub Star / Fork / Issue | **0 / 0 / 0**（仓库创建 06-18，48 天） | — | — |

> **判断：项目处于"重基建、零发行"状态。**代码资产与自动化投入远超同期用户与收入产出，飞轮尚未点火。

### 1.3 线上服务实测（2026-08-04 13:4x 执行）

```
curl https://aishield.tools/api/v1/health   → HTTP 200，响应 1.53s
curl https://aishield.tools/                → HTTP 200
```

**服务是健康的。** 但 `eco/reports/daily-20260803.md` 判定"连续第 11 天不可达"，`weekly-W31.md` 判定"服务中断第 8 天"。这构成本次评估发现的**第二处致命断裂**（详见 2.2）。

---

## 第二部分：五处结构性断裂（附证据）

### 断裂一：调度台账与真实调度器完全脱节 🔴 最高优先级

**证据：**

| 来源 | 声称 | 实测 |
|------|------|------|
| `automation/task-registry.md` | "总计 28 个定时任务"，AIShield 名下 4 个（日 02:00 / 周一 02:00 / 月 1 日 03:00 / 季 03:00），状态均 Active | — |
| `automation/README.md` | 8 个项目分组，全部"运行中" | — |
| WorkBuddy 调度器（`~/.workbuddy/workbuddy.db` → `automations` 表） | — | **仅 1 条记录**：`OracleMind 每日运营健康巡检`，cwd 指向 `Desktop\oraclemind`，与 AIShield **无关** |

**结论：AIShield 在当前可管理的调度器中，定时任务数量为 0。**

台账里的 28 个任务要么运行在另一个不可审计的调度源（报告文件仍在产出，说明确有外部驱动），要么已失效。无论哪种情况，后果相同：

- 任务不可被本机查看、修改、暂停、恢复
- 任务失败无人知晓（无失败通知链路）
- 迁移设备 / 重装工具 = 全部运营自动化静默消失
- **"不需要我参与的项目运营"的第一前提（调度可控）不成立**

---

### 断裂二：监测层持续误报，告警已失效 🔴

**证据链：**

| 时间 | 记录源 | 内容 |
|------|--------|------|
| 07-31 07:31 | `automation/manual-actions.md` | Named Tunnel 部署成功，`aishield.tools/api/v1/health` 返回 200，**问题标记为已解决** |
| 08-03 | `eco/reports/daily-20260803.md` | "Health API 连续第 11 天不可达"，闭环率 **0%**，6 个问题全标 🔴 升级 |
| 08-04 13:4x | 本次实测 | **HTTP 200，服务正常** |

**问题本质：修复动作完成了，但监测侧从未确认修复结果，持续 4 天输出与事实相反的告警。**

连锁后果：
1. 日报「人工介入清单」顶着 5 条 11 天陈旧告警 → 告警疲劳
2. 闭环率被错误计算为 0%，管理指标失真
3. 真实故障发生时，将被淹没在长期误报中（狼来了效应）
4. 周报据此判定"4/6 闭环阻塞，根本原因=服务不可用"，**战略决策建立在错误事实上**

---

### 断裂三：自愈闭环 workflow 存在致命语法错误，从未成功运行 🔴

**文件：** `.github/workflows/self-heal-closed-loop.yml`

**证据：** 该 workflow 定义了 3 个 job — `probe`（第 20 行）、`repair`（第 43 行）、`escalate`（第 102 行）。

```yaml
  escalate:
    name: Escalate to Issue
    needs: verify          # ← 第 104 行
```

`verify` 并非 job，而是 `repair` job 内部的一个 **step id**（`id: verify`）。GitHub Actions 在 workflow 解析阶段即会报错：

```
Job 'escalate' depends on unknown job 'verify'.
```

**这是解析期错误，不是运行期错误** —— 意味着**整个 workflow 自创建以来从未成功执行过一次**。

同时 `escalate` 的条件 `if: needs.verify.result == 'failure'` 同样引用无效对象。

**结论：所谓「监测 → 修复 → 测试 → 部署 → 验证 → 升级」的自愈闭环，在纸面上完整，在实际中完全不存在。** 这直接解释了断裂二为何能持续 4 天无人纠正 —— 本该兜底的自愈与升级机制根本没运行。

---

### 断裂四：情报→功能闭环是空转伪闭环 🟠

**文件：** `.github/workflows/feature-closed-loop.yml`

三个 job 逐一核查：

| Job | 声称职责 | 实际代码行为 | 判定 |
|-----|----------|-------------|------|
| `triage` | 采集可执行 Issue | 筛选后写入 `/tmp/actionable.json` | ⚠️ **跨 job 不共享**，未上传 artifact，下游拿不到 |
| `update-roadmap` | 采纳进 ROADMAP | checkout `dev` 分支后，仅 `echo` 两行标题追加到 ROADMAP.md，**无 git commit / push** | ❌ 改动随 runner 销毁，等于没做 |
| `dispatch-deploy` | 测试并触发部署 | 跑测试后仅 `echo "Feature loop done."` | ❌ 无任何派发动作 |

**结论：情报与用户反馈 → 产品迭代的转化链条完全断裂。** 该 workflow 每周一 03:40 运行，消耗 CI 额度，产出为零。

---

### 断裂五：威胁情报库名不副实，且情报→规则无转化环 🟠

**文件：** `.github/workflows/threat-intel-feed.yml`

| 声称 | 实际 |
|------|------|
| "拉取 NVD / GitHub 安全公告中的 MCP/Agent 相关漏洞" | 实际仅调用 `api.github.com/search/repositories`，按关键词搜**代码仓库**，非 CVE、非 Security Advisory |
| "情报库越全，规则更新越准，检测越强" | 抓取结果只写入 `data/threat_intel.json`，**全流程无任何环节读取该文件生成检测规则** |

**缺失链路：** `情报入库 → 规则生成 → 规则测试 → 规则上线 → 拦截效果验证` — 后 4 环全缺。

对照 `weekly-W31.md` 2.2 节，新增 `sandbox_escape`、`mcp_credential_flow` 两条规则是**人工**在 08-04 提交的（commit `d108022`），并非自动化产出。

---

### 补充断裂：分发链路"生成但从不发出"

| Workflow | 现象 |
|----------|------|
| `channel-distribution.yml` | 内容生成 → commit 到 `content/` ✅；社交发布被 `if [ -n "$TWITTER_BEARER_TOKEN" ]` 门控，token 未配置 → **永远走 else 跳过** |
| `channel-distribution.yml` → registry job | `submit_mcp_directories.py` 仅以**预览模式**运行（注释明写 "预览模式"），**从不真正提交** |

**结论：这解释了"0 registry 上架 / 0 star"的直接原因 —— 内容和提交物都生产了，但发布环节被永久跳过。** `eco/content/` 下躺着 3 篇 08-03 生成的博客，无人可见。

---

## 第三部分：四视角评估

### 3.1 用户视角 — 评分 2/10

| 维度 | 评价 |
|------|------|
| 可发现性 | ❌ 7 个 MCP Registry 全未上架，npm 未发布，0 star。用户在任何入口都搜不到 AIShield |
| 首次体验 | ⚠️ 有 API 和 Web 界面，但无引导式免费试用路径；MCP Server 未上架意味着无法一键接入 |
| 价值感知 | ⚠️ "扫描出安全分"是中间产物，用户真正要的是"我该不该用这个 MCP 工具"的决策结论 |
| 中国用户刚需 | ✅ 违禁词检测是真需求，但**没有独立产品页**，被埋在安全平台里 |
| 信任建立 | ❌ 扫描库仅 21 个工具，用户查询命中率极低，查不到 = 无价值 |

**用户会说的话：** "我搜 MCP 安全扫描，出来的是 Snyk 和 Cisco。AIShield 是谁？"

**最关键动作：** 违禁词检测拆成独立落地页 + 免费额度，这是唯一能自带流量的产品。

---

### 3.2 领域专家视角（AI Agent 安全）— 评分 6/10

**做对的：**

| 判断 | 评价 |
|------|------|
| 锚定 OWASP MCP Top 10 | ✅ 极正确。v0.1 2026 年 2 月才发布，先对齐者获得标准解释权 |
| 中国合规差异化 | ✅ 真实护城河。Snyk / Cisco / Metinc / AgentAuditKit 全部不覆盖违禁词与出海合规 |
| Rug Pull + 握手验证 | ✅ 从"静态配置扫描"走向"行为一致性校验"，方向领先 |
| MCP 2026-07-28 无状态协议适配（handshake.py v4.3） | ✅ 响应及时，双模式回退设计稳妥 |

**做错 / 不足的：**

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 规则未经真实流量验证 | 🔴 | `security_inspections.json` 仅 8 条记录，全部是 07-27 人工构造的测试输入。误报率、漏报率**完全未知** |
| 数据规模是命门 | 🔴 | 21 个工具 vs Metinc 240+ vs 行业 8000+。安全平台的权威性 = 数据覆盖率，21 个意味着 99.7% 查询会 miss |
| 静态扫描正在商品化 | 🟠 | Snyk 免费、Cisco 开源、AgentAuditKit 免费 GitHub Action。**纯静态扫描已无付费意愿** |
| 缺运行时防护 | 🟠 | 周报已识别（Pipelock 48 DLP 模式），但 proxy.py 强化仅停留在"决策"，无执行 |

**领域专家会说的话：** "方向是对的，OWASP 对齐和中国合规是真差异。但你只扫了 21 个工具，这不叫安全数据库，这叫样本。先把数据量做到 500+，权威性自然来。"

---

### 3.3 技术专家视角 — 评分 5/10

**架构层面：**

| 维度 | 评价 |
|------|------|
| 分层设计（安全/信任/经济/协议） | ✅ 清晰合理，符合平台演进路径 |
| A2A / 生态 / Skill 市场 | ❌ 全部内存存储，重启即失。总纲 7.3 已列为"立即清理"项，**至今未清理** |
| Flask(8420) + FastAPI(8450) 双服务 | ❌ 技术债，总纲要求合并，未执行 |
| 单点故障 | 🔴 整个线上服务依赖 **1 条** Cloudflare Named Tunnel。tunnel 挂 = 全站挂，无第二通道 |

**工程质量层面：**

| 问题 | 说明 |
|------|------|
| **CI 不校验 workflow 自身** | 断裂三的语法错误能长期存在，根因在此。`ci.yml` 只跑 `py_compile` 和测试，从不 lint workflow YAML |
| **无人看 Actions 结果** | 一个从未成功运行过的 workflow 存活至今，说明 Actions 运行状态无人监控、无失败通知 |
| 跨 job 数据传递错误 | `feature-closed-loop` 用 `/tmp` 文件跨 job 传数据，是对 Actions 执行模型的基础误解 |
| 测试覆盖 | ✅ 有 `run_all.py` + 4 个测试模块，基础在。但**自动化流程中的测试从不阻断部署**（`continue-on-error: true`） |

**技术专家会说的话：** "你写了 14 个 workflow，但没有一个 workflow 在监控这 14 个 workflow。自动化系统必须先能观测自己。"

---

### 3.4 投资人视角 — 评分 3/10（赛道 8 分，执行 2 分）

**值得投的部分：**

| 要素 | 评估 |
|------|------|
| 赛道 | ✅ AI Agent 市场 113 亿美元（2025）→ 500 亿美元（2030），MCP SDK 月下载 9700 万次 |
| 时机 | ✅ OWASP 标准刚发布、MCP 大版本修订、Agent 支付基础设施 2026 年刚落地 — **三重窗口叠加** |
| 定位 | ✅ "Agent 安全 + 信任 + 中国合规"交叉地带，Metinc 是最近竞品但不做扫描引擎和中国市场 |
| 创始人执行密度 | ✅ 48 天产出 15 个生态模块 + 14 个 workflow + 完整战略文档，工程能力不缺 |

**不能投的部分：**

| 风险 | 严重度 |
|------|--------|
| **零需求验证** | 🔴 48 天，0 star、0 issue、0 真实用户、0 收入。这不是"早期"，是"未验证市场是否存在" |
| **建设/发行严重失衡** | 🔴 投入 14 个自动化 workflow，却有 0 个 registry 上架。**做了 100 分的产能，做了 0 分的渠道** |
| 自动化投入未产生资产 | 🔴 其中 2 个核心 workflow 从未成功运行，1 个空转 —— 自动化投入本身也未验证 |
| 数据护城河未成立 | 🟠 21 个工具样本，距离"数据即壁垒"差 1-2 个数量级 |

**投资人会说的话：**

> "你的问题不是不够努力，是努力用错了地方。过去 48 天你在建工厂，但没卖出一件产品。我不需要看到更多自动化，我需要看到第 1 个愿意付 19 块钱的人。
>
> 建议：冻结所有新功能开发和新定时任务创建，用 2 周只做一件事 —— 把 MCP Server 发到 7 个 Registry，把违禁词检测做成独立落地页。拿到前 10 个真实用户后我们再谈。"

**估值锚点：** 当前状态无法定价。参照 Metinc（240+ 评估、SaaS 订阅在运行），AIShield 需要至少达到 500 工具扫描量 + 50 付费用户，才进入可讨论区间。

---

## 第四部分：Agent 生态趋势预判与生态位卡位

### 4.1 2026 H2 – 2027 五大趋势判断

| # | 趋势 | 依据 | 时间窗 | 对 AIShield 的含义 |
|---|------|------|--------|-------------------|
| T1 | **协议战争收敛**：MCP 锁定工具层，A2A 锁定协作层，其余协议边缘化 | MCP SDK 月下载 9700 万、2026-07-28 无状态大版本；A2A v1.0 落地 | 已发生 | 押注 MCP+A2A 双协议正确，**不要再评估第三协议** |
| T2 | **静态扫描商品化，价值上移到"信任凭证"** | Snyk 免费 / Cisco 开源 / AgentAuditKit 免费 Action，扫描本身已无付费意愿 | 6-12 个月内完成 | **扫描不是产品，认证才是产品**。类比：漏洞扫描器免费，SSL 证书收费 |
| T3 | **Agent 自主支付成为常态，"付款前验安全"成刚需** | 支付宝 3 亿笔 AI 支付、Google AP2（60+ 企业）、x402（55 家金融机构）、Visa TAP | 12-18 个月 | 最大的空白生态位。Agent 要替人花钱，就必须先验证对方可信 |
| T4 | **防护从静态前移到运行时** | Pipelock 运行时 Agent 防火墙、MCP Defender 客户端插件、F5 收购 CalypsoAI | 已启动 | 纯静态扫描的窗口期约 12 个月，之后必须有运行时能力 |
| T5 | **合规从可选变强制** | 中国 AI 合规监管、欧盟 AI Act 分阶段生效 | 12-24 个月 | 中国合规检测将从"引流款"变成"必需品"，这是 AIShield 最被低估的资产 |

### 4.2 关键生态位地图与卡位现状

Agent 生态的价值链条上，有 5 个"收费站"级生态位。AIShield 的现状是**每个都摸到了门，一个都没进去**：

| 生态位 | 本质 | 类比 | AIShield 现状 | 卡位难度 | 建议优先级 |
|--------|------|------|--------------|----------|-----------|
| **① Agent CA（认证机构）** | 发放可被第三方引用的信任凭证 | SSL 证书 / CA 机构 | `badge.py` 代码完备，**0 处外部引用** | 中 | **P0** |
| **② 中国合规网关** | 违禁词 + 出海合规，竞品全盲区 | 等保测评机构 | 能力具备，**无独立产品页** | 低 | **P0** |
| **③ 支付前置安全校验** | Agent 付款链路中插入一道验签 | 支付风控 | 有支付集成，**无此产品形态** | 高 | P1（12 个月布局） |
| **④ CI/CD 安全卡口** | 在 PR 阶段拦截不安全 MCP 依赖 | Snyk / Dependabot | GitHub Action 代码完成，**未上 Marketplace** | 低 | **P0** |
| **⑤ 被调用的安全数据源** | 其他 Agent 调用 AIShield 查工具安全分 | 征信数据接口 | MCP Server 完成，**未上架任何 Registry** | 低 | **P0** |

**核心洞察：**

> 生态位 ①②④⑤ 的卡位难度全部是"低到中"，且代码已完成 80% 以上。
> 阻挡它们的**不是技术，是最后 5% 的发布动作**：npm publish、registry submit、Marketplace 上架、落地页上线。
>
> **这是本次评估最重要的结论 —— AIShield 距离占据 4 个关键生态位，只差几个"发布"按钮，而这些按钮已经卡了 48 天。**

### 4.3 病毒循环的设计缺口

总纲 3.2 设计的「Badge 反链病毒循环」是正确的增长引擎：

```
被扫描工具嵌入 AIShield 徽章 → Agent 爬取发现 → 调用 AIShield → 扫描更多工具
```

但该循环当前**在第 0 步就断了**：没有工具嵌入徽章，因为没有工具被扫描并认证，因为没有用户提交工具，因为产品不可发现。

**破局点：不要等用户来提交。主动扫描 Smithery / Glama TOP 500 工具 → 为每个工具生成公开安全报告页（SEO 入口）→ 主动通知工具作者"你的工具已获 AIShield Silver 认证，可嵌入徽章"。**

这一步把「等待用户」变成「主动供给」，是唯一能冷启动飞轮的路径，且**完全可以自动化**（详见第六部分 N4 任务）。

---

## 第五部分：现有定时任务闭环完整性评估

### 5.1 评估方法

对每个自动化单元，按用户提出的闭环标准逐环核查：

```
监测 → 发现问题 → 诊断 → 修复 → 测试 → 部署上线 → 验证结果 → 未解决则升级 → 通知
```

任一环缺失即判定为**断链**。评分标准：闭环环数 / 应有环数。

### 5.2 板块一：项目自动化监测修复

| 单元 | 频率 | 监测 | 诊断 | 修复 | 测试 | 部署 | 验证 | 升级 | 闭环度 |
|------|------|------|------|------|------|------|------|------|--------|
| `self-heal-closed-loop.yml` | 每 4h | ✅ | ✅ | ✅ | ⚠️ `continue-on-error` 不阻断 | ✅ | ✅ | ❌ **语法错误，job 无效** | **0%（从未运行）** |
| `deploy-server.yml` | 每 4h + push | ✅ 健康门控 | — | ✅ 重部署 | ⚠️ | ✅ | ⚠️ 部分 | ❌ 无 | **50%** |
| AIShield 每日闭环（台账声称） | 每日 02:00 | ⚠️ **误报** | ✅ | ❌ 全部转人工 | ❌ | ❌ | ⚠️ 有跟踪表但基于错误事实 | ⚠️ 写清单无通知 | **25%** |

**板块结论：❌ 不成立。**

三个致命问题叠加：
1. 唯一设计完整的自愈 workflow 因语法错误从未运行
2. 日报监测持续误报，且**误报本身无人检测**
3. 所有"修复"最终都落到「需人工介入清单」，而该清单**没有任何推送机制** —— 写进 md 文件就结束了

> **关键缺口：没有"监测监测者"的机制。** 当监测本身出错（如断裂二），整个体系无法自我纠正。

### 5.3 板块二：自动推广运营 / 功能迭代

| 单元 | 频率 | 生成 | 审核 | 发布 | 数据回收 | 效果验证 | 策略调整 | 闭环度 |
|------|------|------|------|------|----------|----------|----------|--------|
| `channel-distribution.yml` – content | 每周四 06:00 | ✅ | — | ❌ **token 门控，永远跳过** | ❌ | ❌ | ❌ | **17%** |
| `channel-distribution.yml` – registry | 每周四 | ✅ 生成 manifest | — | ❌ **仅 preview 模式** | ❌ | ❌ | ❌ | **17%** |
| `acquisition-automation.yml` | 每周一 + 每日 | ✅ | — | ⚠️ | ⚠️ 有 monitor 脚本 | ❌ | ❌ | **35%** |

**板块结论：❌ 不成立。**

**这是全项目 ROI 最低的板块，也是最致命的板块。** 内容生产能力完备（`eco/content/` 下已有 3 篇成品博客），但发布环节被两道门永久阻断：

- 社交发布：缺 `TWITTER_BEARER_TOKEN` / `REDDIT_CLIENT_ID` → 走 else 分支
- Registry 提交：脚本本身写死为预览模式

**直接后果：0 star / 0 registry / 0 用户。** 断裂五（内容生成但从不发出）与业务指标（1.2 节全线为 0）之间是直接因果关系。

### 5.4 板块三：情报收集 → 分析 → 采纳 → 迭代

| 单元 | 频率 | 采集 | 分析 | 采纳决策 | 转规则/代码 | 测试 | 上线 | 效果验证 | 闭环度 |
|------|------|------|------|----------|------------|------|------|----------|--------|
| `threat-intel-feed.yml` | 每周一/四 07:15 | ⚠️ 仅搜仓库非 CVE | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | **13%** |
| `feature-closed-loop.yml` | 每周一 03:40 | ✅ | ⚠️ 结果丢失 | ❌ **echo 后不提交** | ❌ | ✅ 跑测试 | ❌ 仅 echo | ❌ | **20%** |
| 周报情报闭环（台账声称） | 每周一 | ✅ | ✅ 质量高 | ✅ 有决策 | ❌ **人工手写** | — | — | ⚠️ 列清单无执行 | **40%** |

**板块结论：⚠️ 半成立 —— 情报采集与分析质量高，但采纳与落地 100% 依赖人工。**

`weekly-W31.md` 第二节的威胁情报分析质量相当专业（识别出沙箱逃逸、CVE-2026-16496 凭证流转等），决策也明确（"立即采用"）。但从"决策"到"代码"之间，**没有任何自动化桥梁** —— 两条新规则是人工在 08-04 提交的。

> **关键缺口：分析产物是 Markdown 散文，机器无法消费。** 需要引入结构化决策产物（JSON），才能让下游自动执行。已有 `weekly-decisions-2026-08-03.json` 是正确方向，但无下游消费者。

### 5.5 板块四：技术迭代 / 用户反馈升级

| 单元 | 采集 | 分类 | 排期 | 开发 | 测试 | 发布 | 回访 | 闭环度 |
|------|------|------|------|------|------|------|------|--------|
| `feature-closed-loop.yml`（issues 触发） | ✅ | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | **30%** |
| `issue-labeler.yml` | ✅ | ✅ | — | — | — | — | — | 辅助 |
| `stale.yml` | — | — | — | — | — | — | ⚠️ 自动关闭 | 辅助 |

**板块结论：❌ 不成立，且当前无输入。**

更根本的问题：**Open Issues = 0，仓库 48 天零反馈**。反馈闭环设计得再好，没有输入就是空转。

用户反馈渠道的真实缺口不在"处理"，在"获取"：
- 无用户 → 无反馈 → 无迭代方向 → 产品不改进 → 无用户（负向循环）
- 唯一破局方式：先解决板块二（分发），有了用户才有反馈

### 5.6 板块五：专业数据库对接 + 自建数据库扩展

| 单元 | 频率 | 采集 | 入库 | 去重 | 质量校验 | 报告 | 下游消费 | 闭环度 |
|------|------|------|------|------|----------|------|----------|--------|
| `data-scan-flywheel.yml` | 周二/五 05:30 | ✅ | ✅ | ✅ 增量+skip-days | ❌ | ✅ | ❌ | **60%** |
| `threat-intel-feed.yml` | 周一/四 07:15 | ⚠️ 源不专业 | ✅ | ✅ url 去重 | ❌ | ❌ | ❌ | **35%** |
| `aishield-scan.yml` | 每周一 | ✅ | ⚠️ | — | — | ⚠️ | ❌ | **30%** |

**板块结论：⚠️ 半成立 —— 这是 5 个板块中完成度最高的，但仍缺关键环节。**

`data-scan-flywheel.yml` 逻辑是完整的（爬取 → 扫描 → 增量入库 → 报告 → commit），设计合格。缺口在两端：

- **上游**：未对接真正的专业数据库（NVD API、GitHub Security Advisory GraphQL、OSV.dev、CNVD）
- **下游**：入库后无消费者。扫描到高危工具**不触发**：规则更新、公开报告页生成、工具作者通知、徽章签发

> 数据飞轮转起来了，但飞轮没有连接任何传动轴。

### 5.7 五大板块总评

| 板块 | 闭环度 | 判定 | 阻断点 |
|------|--------|------|--------|
| ① 自动化监测修复 | **25%** | ❌ 不成立 | workflow 语法错误 + 监测误报 + 无通知 |
| ② 自动推广运营 | **23%** | ❌ 不成立 | 发布环节被 token/preview 双重阻断 |
| ③ 情报采纳迭代 | **24%** | ⚠️ 半成立 | 决策产物非结构化，无自动执行桥梁 |
| ④ 技术迭代/用户反馈 | **30%** | ❌ 不成立 | 无输入（0 issue），依赖板块② |
| ⑤ 数据库对接扩展 | **42%** | ⚠️ 半成立 | 上游源不专业，下游无消费者 |
| **整体** | **~29%** | **❌ 距离无人值守运营还有约 70% 的路** | — |

### 5.8 全局性缺陷（跨板块）

除各板块自身问题外，存在 4 个**贯穿所有板块**的系统性缺陷：

| # | 缺陷 | 后果 | 严重度 |
|---|------|------|--------|
| G1 | **无元监控**（无人监控自动化系统本身） | workflow 语法错误长期存活、误报持续 4 天 | 🔴 |
| G2 | **无通知出口**（所有告警终点是 md 文件） | 即使发现问题也传不到人，"需人工介入清单"事实上无人读 | 🔴 |
| G3 | **无结构化状态中枢**（状态散落在 md 散文中） | 机器无法消费上游产出，每个环节都要人工翻译 | 🟠 |
| G4 | **测试不阻断**（`continue-on-error: true`） | 测试失败照样部署，测试环节形同虚设 | 🟠 |

---

## 第六部分：补位方案

### 6.0 顺序原则（重要）

**不要先加新定时任务。** 在断裂一（调度台账失真）、断裂三（workflow 语法错误）、G2（无通知出口）修复之前，新增任务只会：

- 增加更多不可见、不可管理的运行单元
- 制造更多写进 md 文件却无人阅读的报告
- 让"28 个任务"变成"40 个任务"，而真实生效的仍然是少数

**正确顺序：先修复 → 再补位 → 后扩展。**

---

### 6.1 阶段 0：止血修复（必须先做，3 项）

| # | 修复项 | 具体动作 | 工作量 |
|---|--------|----------|--------|
| **F1** | 修复自愈 workflow 语法错误 | `self-heal-closed-loop.yml`：将 `repair` job 拆分为 `repair` + `verify` 两个 job，或把 `escalate` 的 `needs: verify` 改为 `needs: repair` + `if: needs.repair.result == 'failure'` | 10 分钟 |
| **F2** | 打通通知出口 | 所有告警接入实际可达渠道（已连接 Agent Mail，可直接用邮件；或 GitHub Issue + 企微机器人）。**规则：任何 P0 告警必须离开文件系统** | 1 小时 |
| **F3** | 校正调度台账 | 用调度器实际数据重写 `automation/task-registry.md`，标注每个任务的真实归属调度源；不可管理的任务迁移到可管理调度器 | 30 分钟 |

> F1 是 10 分钟的修复，但它挡住了整个"监测→修复→验证→升级"闭环 48 天。**这是本次评估投入产出比最高的一项。**

---

### 6.2 阶段 1：补位定时任务清单（10 项）

按优先级排序。标注 ⭐ 的为构成无人值守闭环的**必需项**。

#### N1 ⭐ 自动化体系元监控（Meta-Monitor）

| 项 | 内容 |
|----|------|
| **解决** | G1 无元监控、断裂三无人发现 |
| **频率** | 每日 08:00（`FREQ=DAILY;BYHOUR=8`） |
| **职责** | ① 用 `actionlint` 校验全部 14 个 workflow YAML 语法；② 拉取近 24h Actions 运行记录，统计成功/失败/**从未运行**；③ 对比 `task-registry.md` 与调度器实际记录，报告差异；④ 检测"连续 N 天产出相同结论"的疑似卡死任务 |
| **闭环** | 发现语法错误 → 自动提 PR 修复 → 测试 → 合并 → 复检 → 仍失败则邮件升级 |
| **为何必需** | **这是唯一能防止本次评估问题重演的任务。**没有它，自动化系统无法自我纠错 |

#### N2 ⭐ 监测事实校验（Fact-Check Loop）

| 项 | 内容 |
|----|------|
| **解决** | 断裂二 监测误报持续 4 天 |
| **频率** | 每 2 小时（`FREQ=HOURLY;INTERVAL=2`） |
| **职责** | ① 从 **3 个独立路径**探测服务（Cloudflare 边缘 / 直连源站 / 第三方 uptime 服务），三取二判定；② 与最近一次日报结论比对，**不一致立即告警"监测系统可能失准"**；③ 结果写入结构化 `data/service_status.json`（带时间戳与探测路径明细） |
| **闭环** | 探测异常 → 触发 self-heal → 验证 → 恢复则自动关闭告警并**回写修正日报结论** |
| **为何必需** | 单点探测 + 无交叉验证 = 误报无法自我发现。三路探测是最低成本的解法 |

#### N3 ⭐ 发布执行器（Release Executor）

| 项 | 内容 |
|----|------|
| **解决** | 断裂六 内容/提交物生成但从不发出、板块② 23% 闭环度 |
| **频率** | 每日 10:00（`FREQ=DAILY;BYHOUR=10`） |
| **职责** | ① 扫描 `content/`、`eco/content/` 下**未发布**的产物（用 `data/publish_ledger.json` 记录发布状态）；② 逐个执行真实发布：Registry 提交（去掉 preview 模式）、博客上线、社交发帖；③ 缺 token 的渠道**不静默跳过**，而是产出"缺失凭据清单"并邮件告知；④ 发布后回写 ledger + 采集 24h 后的曝光数据 |
| **闭环** | 生成 → 发布 → 记录 → 回收数据 → 效果不佳则调整下周选题 |
| **为何必需** | 这是把「0 star / 0 registry」变成非零的唯一执行器。**当前项目最短缺的不是产能，是发行。** |

#### N4 ⭐ 数据飞轮点火器（Cold-Start Flywheel）

| 项 | 内容 |
|----|------|
| **解决** | 4.3 病毒循环第 0 步断裂、领域专家指出的"21 个工具"命门 |
| **频率** | 每日 04:00（`FREQ=DAILY;BYHOUR=4`），持续至库存 ≥ 500 |
| **职责** | ① 从 Smithery / Glama / PulseMCP 拉取热度 TOP 工具，每日扫描 20-30 个；② 为每个工具生成**公开可索引的安全报告页**（SEO 入口）；③ 达标工具自动签发徽章；④ 通过 GitHub Issue / 仓库邮箱**主动通知作者**"你的工具已获 AIShield 认证，可嵌入徽章"；⑤ 追踪徽章嵌入率 |
| **闭环** | 扫描 → 报告页 → 通知作者 → 徽章嵌入 → 反链引流 → 新工具提交 → 扫描 |
| **为何必需** | 把"等用户来"改成"主动供给"，是冷启动飞轮的唯一自动化路径。同时直接解决数据量命门（500 工具约需 20 天） |

#### N5 ⭐ 情报→规则转化器（Intel-to-Rule）

| 项 | 内容 |
|----|------|
| **解决** | 断裂五 情报无转化、板块③ 决策无执行桥梁 |
| **频率** | 每周二、五 09:00（`FREQ=WEEKLY;BYDAY=TU,FR;BYHOUR=9`） |
| **职责** | ① 读取 `data/threat_intel.json` 中未处理条目；② 用本地 Ollama 模型（零 API 成本）分析威胁 → 生成候选检测规则（正则/语义特征）；③ 用历史扫描样本做**误报率回归测试**（误报率 >5% 则拒绝）；④ 通过则自动提 PR，CI 通过后合并上线；⑤ 上线后 7 天追踪该规则实际拦截数，0 拦截则标记待复核 |
| **闭环** | 情报 → 规则 → 回归测试 → PR → 上线 → 效果验证 → 无效则回滚 |
| **为何必需** | 这是「专业数据库 → 产品能力」的传动轴。没有它，情报库只是一堆 JSON |

#### N6 专业数据库正式对接（Pro-DB Ingestion）

| 项 | 内容 |
|----|------|
| **解决** | 断裂五 数据源不专业（当前只搜 GitHub 仓库） |
| **频率** | 每日 06:00（`FREQ=DAILY;BYHOUR=6`） |
| **职责** | 对接真正的专业源：**NVD API 2.0**（CVE）、**GitHub Security Advisory GraphQL**、**OSV.dev**（开源漏洞）、**CNVD/CNNVD**（中国国家漏洞库，中国合规差异化必需）；按 MCP/Agent/LLM 关键词过滤入库；标注 CVSS 评分与受影响版本 |
| **闭环** | 采集 → 去重入库 → 交给 N5 转规则 → 高危 CVE 触发存量工具**重扫** |
| **备注** | CNVD 对接是竞品盲区，直接强化中国市场护城河 |

#### N7 用户反馈获取器（Feedback Acquisition）

| 项 | 内容 |
|----|------|
| **解决** | 板块④ 无输入（0 issue） |
| **频率** | 每周三 10:00（`FREQ=WEEKLY;BYDAY=WE;BYHOUR=10`） |
| **职责** | 反馈不能等，要主动采集：① 监听 Reddit r/mcp、r/LocalLLaMA、HN、V2EX、知乎中 MCP 安全相关讨论；② 提取用户抱怨的真实痛点；③ 转为 GitHub Issue（标 `from-community`）进入需求池；④ 对已发布内容下的评论自动生成回复草稿 |
| **闭环** | 采集 → 提炼 → 入需求池 → 排期 → 开发 → 发布 → 回帖告知提出者 |

#### N8 变现健康监控（Revenue Watch）

| 项 | 内容 |
|----|------|
| **解决** | 盈利化原则无监控单元；OracleMind 已有同类任务，AIShield 缺 |
| **频率** | 每日 09:00（`FREQ=DAILY;BYHOUR=9`） |
| **职责** | ① 支付链路端到端探活（Creem checkout 创建 → webhook 回调完整性）；② 积分消耗率、充值/消费比、402 拒绝数；③ **免费额度耗尽用户识别 → 自动触发升级引导邮件**；④ 首笔真实付费达成时立即通知 |
| **闭环** | 监测 → 异常自动重试/降级 → 验证 → 未恢复则升级；转化机会 → 自动触达 → 追踪转化 |

#### N9 竞品与生态位追踪（Competitive Radar）

| 项 | 内容 |
|----|------|
| **解决** | 总纲 6.1 技术雷达要求，当前靠周报人工完成 |
| **频率** | 每周一 07:00（`FREQ=WEEKLY;BYDAY=MO;BYHOUR=7`） |
| **职责** | 追踪 Snyk / Cisco / Metinc / AgentAuditKit / Pipelock 的 release、定价、star 增速；对比 AIShield 规则数与覆盖面差距；**生态位占领度打分**（第 4.2 节 5 个生态位逐一评分）；差距 >阈值 则自动生成 ROADMAP 条目 |
| **闭环** | 追踪 → 差距量化 → 生成任务 → 进 ROADMAP → 排期 → 复检 |

#### N10 周度决策执行器（Decision Executor）

| 项 | 内容 |
|----|------|
| **解决** | G3 决策产物非结构化、周报"下周验证清单"从无人执行 |
| **频率** | 每周一 12:00（`FREQ=WEEKLY;BYDAY=MO;BYHOUR=12`），在周报生成后 |
| **职责** | ① 解析 `weekly-decisions-YYYY-MM-DD.json`（**要求周报必须同时产出结构化决策 JSON**）；② 逐条决策派发到对应执行器（规则类→N5，内容类→N3，研发类→提 Issue）；③ 追踪上周决策执行率；④ 连续 2 周未执行的决策自动升级或废弃 |
| **闭环** | 决策 → 派发 → 执行 → 回收结果 → 未执行则升级 |
| **为何关键** | 这是把"分析型自动化"接到"执行型自动化"的总线。没有它，周报再专业也只是文档 |

---

### 6.3 补位任务优先级与依赖

| 优先级 | 任务 | 依赖 | 上线时机 |
|--------|------|------|----------|
| **P0** | F1 F2 F3（止血） | 无 | 立即，1 天内 |
| **P0** | N1 元监控 | F1 F2 | 第 2 天 |
| **P0** | N2 事实校验 | F2 | 第 2 天 |
| **P0** | N3 发布执行器 | F2 + 配置社交/Registry 凭据 | 第 3 天 |
| **P1** | N4 飞轮点火 | N3 | 第 1 周内 |
| **P1** | N5 情报转规则 | N6 | 第 2 周 |
| **P1** | N6 专业库对接 | 无 | 第 2 周 |
| **P2** | N8 变现监控 | 服务稳定 | 第 3 周 |
| **P2** | N10 决策执行器 | 周报改造为结构化输出 | 第 3 周 |
| **P3** | N7 反馈获取 / N9 竞品雷达 | N3 | 第 4 周 |

---

## 第七部分：无人值守运营的三层架构

补位任务不能是平铺的 10 个孤立单元，必须组织成有层级的自治系统：

```
┌──────────────────────────────────────────────────────────┐
│  L3 元层（自动化系统的自动化）                              │
│  N1 元监控  ——  监控 workflow 健康、检测卡死、校验台账      │
│  职责：确保 L1/L2 真的在运行。发现问题自动修 PR             │
└────────────────────────┬─────────────────────────────────┘
                         │ 监控
┌────────────────────────▼─────────────────────────────────┐
│  L2 决策层（分析 → 结构化决策 → 派发）                      │
│  周报生成 → weekly-decisions.json → N10 决策执行器          │
│  N9 竞品雷达 / N5 情报转规则 / N7 反馈获取                  │
│  职责：把观察变成机器可执行的指令                            │
└────────────────────────┬─────────────────────────────────┘
                         │ 派发指令
┌────────────────────────▼─────────────────────────────────┐
│  L1 执行层（真实副作用发生的地方）                          │
│  N2 事实校验 / self-heal 自愈 / N3 发布执行 / N4 飞轮点火    │
│  N6 数据入库 / N8 变现监控                                 │
│  职责：改变外部世界的状态（部署、发布、入库、通知）          │
└──────────────────────────────────────────────────────────┘

         ╔══════════════════════════════════════╗
         ║  贯穿三层的两条总线（当前完全缺失）    ║
         ║  ① 通知总线：任何 P0 必须离开文件系统  ║
         ║  ② 状态总线：结构化 JSON 而非 md 散文  ║
         ╚══════════════════════════════════════╝
```

### 7.1 状态总线设计（G3 的解法）

建议在 `data/` 下建立机器可读的状态中枢，取代散落在 md 中的结论：

| 文件 | 内容 | 生产者 | 消费者 |
|------|------|--------|--------|
| `data/service_status.json` | 多路探测结果、健康历史 | N2 | 日报、self-heal、N1 |
| `data/publish_ledger.json` | 每项内容/提交物的发布状态与效果 | N3 | 周报、N9 |
| `data/open_issues.json` | 统一问题池（含首次发现时间、当前状态、升级次数） | 全部 L1 | N1、N10、通知总线 |
| `data/weekly-decisions.json` | 结构化决策（已有雏形，需规范 schema） | 周报 | N10 |
| `data/rule_effectiveness.json` | 每条规则的上线时间、拦截数、误报数 | N5 | N5 自身回归 |

**规则：md 报告只作为人类可读的呈现层，机器间传递一律走 JSON。**

### 7.2 通知总线设计（G2 的解法）

| 级别 | 触发条件 | 渠道 | 时限 |
|------|----------|------|------|
| **P0** | 服务不可达 >30min、支付链路断、自愈失败、数据丢失 | 邮件（Agent Mail 已连接）+ GitHub Issue | 立即 |
| **P1** | 单个 workflow 连续 2 次失败、规则误报率超阈值、发布失败 | 邮件日汇总 | 24h 内 |
| **P2** | 决策连续 2 周未执行、竞品差距扩大、库存增长停滞 | 周报汇总 | 每周 |

**当前状态：全部三级都只写入 md 文件，实际送达率 0。这是"无人值守"能否成立的分水岭 —— 无人值守不等于无人知晓。**

---

## 第八部分：90 天执行序列

| 阶段 | 时间 | 目标 | 关键动作 | 验收标准 |
|------|------|------|----------|----------|
| **止血** | D1–D3 | 让现有自动化真的能跑 | F1 修 workflow / F2 通通知 / F3 正台账 | self-heal 首次成功运行；一封告警邮件真实送达 |
| **元层** | D4–D7 | 系统能自我监控 | 上线 N1 + N2 | 元监控报出至少 1 个真实问题；误报归零 |
| **发行** | W2–W3 | **从 0 到 1 的分发突破** | 上线 N3；npm publish；7 个 Registry 实际提交；违禁词独立落地页 | ≥5 个 Registry 上架；npm 可安装；首个非自己的 star |
| **飞轮** | W4–W6 | 数据量突破权威阈值 | 上线 N4 + N6；主动扫描并通知作者 | 扫描库 ≥300 工具；≥10 个徽章嵌入 |
| **传动** | W7–W9 | 情报与决策自动落地 | 上线 N5 + N10；周报改结构化输出 | 至少 3 条规则由自动化产出并上线 |
| **变现** | W10–W13 | 验证付费意愿 | 上线 N8 + N7；免费额度耗尽触发升级引导 | **首笔真实付费收入**；≥10 个真实注册用户 |

### 8.1 判定"无人值守"是否达成的 6 条硬标准

在 90 天末，逐条核查：

| # | 标准 | 当前 | 目标 |
|---|------|------|------|
| 1 | 服务异常能自动修复并验证，无需人工 | ❌ | ✅ |
| 2 | 任何 P0 告警能在 30 分钟内送达手机 | ❌ | ✅ |
| 3 | 内容与提交物能自动真实发布，不被静默跳过 | ❌ | ✅ |
| 4 | 情报能自动转为上线的检测规则 | ❌ | ✅ |
| 5 | 自动化系统本身被监控，语法错误/卡死能自动发现 | ❌ | ✅ |
| 6 | 台账与真实调度器 100% 一致 | ❌ | ✅ |

**当前 0/6。这 6 条全部达成，才叫"不需要你参与的项目运营"。**

---

## 第九部分：一句话结论

> **AIShield 的赛道判断、架构设计、战略文档都达到了专业投资级水准，但整个体系卡在"最后 5% 的发布动作"上 48 天。**
>
> 当前不缺自动化设计，缺的是三样东西：
> 1. **让已有的自动化真的能运行**（一个 10 分钟的语法修复挡了 48 天）
> 2. **让告警真的能送达人**（所有告警终点都是没人读的 md 文件）
> 3. **让生成的东西真的能发出去**（内容和提交物全被门控静默跳过）
>
> 修好这三点，再上 10 个补位任务，无人值守运营才有物理基础。
> **在此之前增加任何新定时任务，都只是在增加不可见的空转。**

---

*评估人：WorkBuddy | 评估方式：全目录静态扫描 + 调度器数据库直查 + 线上服务实测 + workflow 逐行核查*
*本报告结论均附证据，可逐条复核*




