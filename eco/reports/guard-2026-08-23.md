# AIShield 安全守夜报告 · 2026-08-23

> 北京时间 08:30 自动运行 · automation-1786840209961
> 结论：**三项全绿，无 🔴 阻断异常**

## 总览

| 门禁项 | 结果 | 关键指标 |
|--------|------|----------|
| ① 对外发布物自检（项目自带扫描器） | ✅ 通过 | 5/5 通过 · 评分 94–97 · 0 阻断项 |
| ② 线上存活（Contents API + 站点探测） | ✅ 存活 | 健康探针 score 4/4、http 200；站点 HTTP=200 |
| ③ 测试门禁（tests/run_all.py） | ✅ 全绿 | 557 通过 / 0 失败 / 0 错误 / 12 跳过 |

---

## ① 对外发布物安全自检

运行 `python scripts/verify_distribution.py`，复用 scanner 引擎（rules/dependency/secrets/poisoning/taint）对 `distribution/` 下全部对外发布物做只读扫描，**绝不执行发布物内命令、绝不发起网络请求**。

**结论：全部通过（共 5 条）** — 退出码 0

| 渠道 | 产物 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | rebuilt | 2 | 97 | 0 | 通过 |
| claude-skills | aishield-security-scan | in_repo | 1 | 96 | 0 | 通过 |
| gpt-store | aishield-gpt | in_repo | 1 | 95 | 0 | 通过 |
| huggingface | aishield-agent-security-benchmark | in_repo | 1 | 94 | 0 | 通过 |
| clawhub | aishield | in_repo | 1 | 94 | 0 | 通过 |

**非阻断提示（持续）**：agensi 线上版本 1.0.0 与仓库源版本 1.1.0 不一致，自 08-17 起持续提醒，下次更新需覆盖发布。属版本台账差异，非安全洞。

---

## ② 线上存活核查

### ②a 远端 `data/state/health.json`（GitHub Contents API 直读，本地副本不算数）

- 读取成功，blob sha `65de33335d`
- `current.checked_at = 2026-08-22T23:24:16Z`（≈北京时间 08-23 07:24，约 1h06m 前，在探针小时级节奏内）
- `current.score = 4 / max_score = 4`，`http = 200`，`consecutive_failures = 0`，`healthy = true`
- ⚠️ 字段漂移提示（非异常）：健康 schema 由旧版 `updated` 改为 `checked_at` + `current.checked_at`（典型文档/代码漂移，与项目「真相源=代码、文档屡漂移」现状一致）。探针判定为健康，无需处理。

### ②b `aishield.tools` 首页 HTTP 状态

- `HTTP=200`，耗时 1.30s
- curl 退出码 23 = 写 `/dev/null` 的本地 TLS 代理小怪癖，**不影响在线判定**（站点全球存活，远端健康探针 http 200 / score 4/4 为旁证）

---

## ③ 测试门禁

运行 `python tests/run_all.py`：

```
Total:    557
Passed:   557
Failed:   0
Errors:   0
Skipped:  12
```

- 失败数恒 0，错误数恒 0
- **核心契约保持**：良性零误报 + 恶意仍拦 40 项（在 CI 契约用例内验证）

---

## 运维备注

- 未登录任何外部平台、未替用户发布或发邮件（守夜硬约束）。
- 本报告经 `scripts/gh_push.py` 推 `main`，并用 Contents API 复验。
- agensi 版本台账差异（1.0.0 vs 1.1.0）仍挂起，建议近期覆盖发布以消除持续提示。
