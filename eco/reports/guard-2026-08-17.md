# AIShield 每日安全守夜报告 — 2026-08-17

> 执行时间（北京时间）：2026-08-17 08:20
> 执行身份：AIShield 每日安全守夜 agent（automation-1786840209961）
> 模式：持续鉴证 + 自带洞必检 + 健康门禁。未登录任何外部平台、未发布、未发邮件。

## ⚠️ 顶部提醒（异常高亮）

| 级别 | 项 | 结论 | 说明 |
|------|----|------|------|
| 🟡 需复核 | **aishield.tools 首页可达性** | **本机探测不可达（HTTP=000 / TCP 超时）** | 从守夜主机 TCP:443 连接 aishield.tools 12–22s 超时；对照 api.github.com 可达（HTTP=200）、DNS 正常解析到 Cloudflare（172.67.188.44 / 104.21.81.46）。**最可能是本机 TLS 代理出口未放行该域名（环境限制），而非全球宕机**。请用户在自有浏览器打开 https://aishield.tools/ 确认线上状态。 |
| 🟢 正常 | 对外发布物自检 | 全过 | 5/5 通过，0 高危未修。 |
| 🟢 正常 | 远端 health.json | 存活 | `updated = 2026-08-16T23:23:23Z`（约 1 小时内，远未失活）。 |
| 🟢 正常 | 测试套件 | 全绿 | 523 通过 / 0 失败 / 9 跳过，与基准一致。 |

> 无 🔴 阻断级异常。唯一需用户侧人工复核项为 aishield.tools 首页可达性（受本机网络出口限制，无法从守夜主机确证）。

---

## 1. 对外发布物安全自检（distribution gate）

执行：`python scripts/verify_distribution.py`（复用 scanner 引擎，只读、绝不执行发布物命令、不发起网络请求）

**结论：全部通过（共 5 条）** ｜ 策略：最低分 80，阻断等级 critical/high，要求源留底=是 ｜ 退出码 0

| 渠道 | 产物 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | rebuilt | 2 | 97 | 0 | 通过 |
| claude-skills | aishield-security-scan | in_repo | 1 | 96 | 0 | 通过 |
| gpt-store | aishield-gpt | in_repo | 1 | 95 | 0 | 通过 |
| huggingface | aishield-agent-security-benchmark | in_repo | 1 | 94 | 0 | 通过 |
| clawhub | aishield | in_repo | 1 | 94 | 0 | 通过 |

明细提示（非阻断）：
- **agensi/chinese-seo-compliance**：线上版本 1.0.0 与仓库源版本 1.1.0 不一致，下次更新需覆盖发布（仅提示，不阻断）。

不变量校验：✅ 只读扫描，不执行发布物中的任何命令，不发起网络请求。

---

## 2. 线上存活探测

### 2.1 远端 health.json（GitHub Contents API，本地副本不算数）
- 请求：`GET api.github.com/repos/lm203688/aishield/contents/data/state/health.json`（curl --ssl-no-revoke --tlsv1.3，Bearer PAT）
- HTTP 状态：**200**
- `updated = 2026-08-16T23:23:23.947451+00:00`（≈ 当前守夜时刻的 1 小时内，存活信号有效）
- 其余字段：domain / current / history / desc 均存在。

### 2.2 aishield.tools 首页
- 请求：`GET https://aishield.tools/`（curl --ssl-no-revoke --tlsv1.3）
- 结果：**HTTP=000，TCP:443 连接 12–22s 超时（curl exit 28）**
- 对照实验：
  - `nslookup aishield.tools` → 正常解析至 Cloudflare（172.67.188.44 / 104.21.81.46，含 IPv6）
  - `GET https://api.github.com` → **HTTP=200**（证明 TLS 代理链路本身工作）
- **判定**：本机出口对 aishield.tools 不可达，但 DNS 正常、同源 Cloudflare 的 github 可达 → 倾向「代理出口白名单未含 aishield.tools」这一环境限制，不能据此判定站点全球宕机。
- **行动建议**：请用户在自有网络/浏览器打开 https://aishield.tools/ 与 https://aishield.tools/banned-words 核验；若用户侧同样不可达，则确证线上失活，需走 CF Pages 重建链路（历史遗留 cfut_ token 权限问题）。

---

## 3. 测试套件

执行：`python tests/run_all.py`（20/20 测试模块已加载）

```
Ran 523 tests in 30.830s
OK (skipped=9)

Total:    523
Passed:   523
Failed:   0
Errors:   0
Skipped:  9
ALL TESTS PASSED
```

- **基准对照**：523 通过 / 0 失败 / 9 跳过 —— 与既定基准完全一致 ✅
- **核心契约验证**：`test_capability_full_scan` 覆盖的「良性零误报 + 恶意仍拦 40 项」用例全部 `... ok`（含 `test_benign_no_false_positive` ×3、`test_*_flagged` 恶意拦截用例），门禁未退化。

---

## 4. 综合结论

| 维度 | 状态 | 证据 |
|------|------|------|
| 对外发布物安全 | 🟢 通过 | distribution gate 退出码 0，5/5，评分 94–97，0 阻断 |
| 远端状态存活 | 🟢 存活 | health.json updated 在 1h 内 |
| 线上站点可达 | 🟡 本机不可达（待复核） | aishield.tools TCP 超时；疑似代理出口限制 |
| 测试健康 | 🟢 全绿 | 523/0/9，基准对齐 |

**守夜 agent 未做不可逆动作**（仅读扫描、只读 API、未推送任何外部平台）。本报告文件已写入 `eco/reports/guard-2026-08-17.md`，按约束由 gh_push.py 推 main 并以 Contents API 复验。

> 唯一需用户决策项：aishield.tools 首页可达性复核（环境网络限制导致无法从守夜主机确证）。
