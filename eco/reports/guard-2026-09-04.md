# AIShield 每日安全守夜报告 — 2026-09-04

- 运行时间：2026-09-04 08:30 CST（automation-1786840209961）
- 结论：**本地三项 PASS，但发现一条被长期掩盖的关键异常 —— CI 上测试红灯已阻断线上部署 3 天。**

| 检查项 | 结论 | 关键数据 |
| --- | --- | --- |
| 1. 对外发布物安全自检 | PASS | 5/5 通过，评分 94–97，0 阻断项 |
| 2. 线上存活 | PASS | 远端 health.json `updated` = 2026-09-03T21:08:55Z；aishield.tools HTTP 200 |
| 3. 本地测试 | PASS | 592 通过 / 0 失败 / 12 跳过 |
| ⚠️ 附加发现 | **FAIL** | GitHub Actions 上 `tests/run_all.py` 红灯 → spine 部署链路被闸门挡下，线上进程停在 09-01 的 commit |

---

## 1. 对外发布物安全自检（PASS）

`scripts/verify_distribution.py --json`，策略 `min_overall_score=80` / 阻断 `critical+high` / 要求源在仓内。

| 渠道 | 产物 | 版本 | 源状态 | 评分 | 阻断项 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| agensi | chinese-seo-compliance | 1.1.0 | rebuilt | 97 | 0 | PASS |
| claude-skills | aishield-security-scan | 4.2.2 | in_repo | 96 | 0 | PASS |
| gpt-store | aishield-gpt | 4.2.2 | in_repo | 95 | 0 | PASS |
| huggingface | aishield-agent-security-benchmark | 4.2.2 | in_repo | 94 | 0 | PASS |
| clawhub | aishield | 4.2.2 | in_repo | 94 | 0 | PASS |

- 不变式：`no_execute=true`、`no_network=true`（未 spawn 被扫配置内任何命令）。
- 唯一提示项（非阻断）：agensi 线上 1.0.0 落后仓库源 1.1.0，下次更新需覆盖发布。
- **无高危未修项。**

## 2. 线上存活（PASS）

| 探测 | 结果 |
| --- | --- |
| 远端 `data/state/health.json`（Contents API, ref=main） | HTTP 200，blob `ebe9e0bc`，`updated` = **2026-09-03T21:08:55Z**（运行前 ≈3.4h，自愈闭环仍在结算） |
| `https://aishield.tools/` | **HTTP 200**，28,509 B，1.43s |
| `https://aishield.tools/api/v1/health` | **HTTP 200**，`status=ok`，`version=4.3.0`，`rules_count=228`（static 204 / generated 9 / radar 15） |

线上服务本身健康，规则数与线上真值 228 一致。

**但部署新鲜度不合格**：线上 `commit=93fcd10c`、`deployed_at=2026-09-01T05:00:07Z`，而 main HEAD 已是 `a2fa71f4`（2026-09-03T21:09:09Z）。按「运行 commit ≠ 磁盘 commit 即强制重启」判据，线上进程已陈旧 3 天——09-01 之后新增的规则与情报未真正生效。根因见第 4 节。

## 3. 本地测试（PASS）

`python tests/run_all.py`：加载 27/27 模块，**592 通过 / 0 失败 / 0 错误 / 12 跳过**（5.17s），高于基准 523/0/9。核心契约（良性零误报 + 恶意仍拦）通过。

## 4. ⚠️ 关键异常：CI 红灯阻断部署链路（本地绿 ≠ 线上绿）

### 现象

近 20 次 workflow run 中有 6 次 failure，集中在两条链：

| Workflow | 失败 run | 失败 job / step |
| --- | --- | --- |
| AIShield Closed-Loop Spine | 09-03T07:59（860eacab） | `2. 威胁情报采集 → 3-Verify Scanner Integrity` / **Run full test suite** |
| AIShield CI/CD | 09-02T18:28、09-03T00:32 ×2、09-03T05:25、09-03T05:31 | `test` / **Run all tests** |

spine 失败后，下游 **`4-Commit Intel & Rules`、`3. 规则晋升闸门`、`4. 自扫描`、`5. CI 验证`、`6. 部署（验证后才上线）`、`8. 对外分发`、`9. 迭代汇报` 全部 skipped** —— 部署 job 从未执行，这正是线上 commit 停在 09-01 的直接原因。闸门本身工作正常（该拦就拦），问题是被拦的东西一直没人修。

### 根因（已定位到单文件单处）

CI 日志（run 33719194650, job 100534650656）：

```
FAIL: test_life_science_papers_excluded (tests.test_tech_radar.TestCapabilityGap)
AssertionError: True is not false : 临床试验论文不应进入采纳清单（risk of bias 非安全语义）
Total: 592  Passed: 591  Failed: 1  Skipped: 12
```

对比本地与远端 main（git blob sha）：

| 文件 | 远端 sha | 本地 sha | 判定 |
| --- | --- | --- | --- |
| `tests/test_tech_radar.py` | d1c26daf | d1c26daf | 一致（测试已在远端） |
| `scripts/capability_gap.py` | ab1ada28（15,778 B） | 2debf666（16,406 B） | **不一致：本地含修复，远端缺** |

差异仅一处：本地 `OUT_OF_SCOPE` 黑名单多出 12 条生命科学/材料学排除词（`clinical trial`、`risk of bias`、`systematic review`、`biomedical`、`drug discovery`、`genomic`、`protein folding` 等）。本地文件 mtime = **2026-08-11 08:48** —— 该修复 8 月 11 日就写好了，但从未推到 main，导致 CI 上这条测试长期红。

### 为什么之前三天守夜没抓到

守夜只跑本地 `tests/run_all.py`（本地含修复 → 绿），未核对 GitHub Actions 上的结论。加上历史上把 CI FAIL 误判为「改名前残影」，真身 `AIShield CI/CD` 的失败被掩盖。

### 处置

1. 已将 `scripts/capability_gap.py`（含 8-11 修复）推送 main，让远端与本地一致；
2. 推送触发 CI 后需复验 `test` job 转绿，spine 下次 03:17 UTC 运行时部署 job 才会解锁；
3. **守夜流程需加一项断言**：除本地测试外，必须读 GitHub Actions 最近一次 `AIShield CI/CD` 与 `Closed-Loop Spine` 的 conclusion，任一 failure 即报警。本地绿不能替代 CI 绿。

### 附：其他本地/远端漂移（观察项，非阻断）

以 main HEAD `a2fa71f4` 全树比对，`scripts/ tests/ scanner/ eco/ api/` 下 21 个文件本地与远端不一致、10 个远端有本地无。绝大多数是远端自动化 commit（本地无 git 仓、不会自动同步）造成的本地陈旧，方向与本案相反，不影响 CI；但 `eco/agent_gateway.py`、`eco/payment.py`、`eco/x402.py`、`scanner/sbom.py`、`scripts/intel_to_rules.py` 等需另开一轮逐个定向确认漂移方向，避免同类「本地修了没推」再次潜伏。

---

## 结论

- 铁律「对外发布物必须过自家扫描器」：**满足**，5/5 通过、0 高危未修。
- 线上未失活，但**部署陈旧 3 天**，根因是 CI 测试红灯（远端缺一处 8-11 的修复）阻断 spine 部署闸门。
- 已推送修复；须复验 CI 转绿 + 线上 commit 追平 main HEAD。
