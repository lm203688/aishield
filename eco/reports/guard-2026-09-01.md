# AIShield 每日安全守夜报告（guard-2026-09-01）

> 运行时间：2026-09-01 08:30 (CST, UTC+8)
> 模式：持续鉴证 + 自带洞必检 + 健康门禁
> 结论：**三项全部 PASS，无关键异常**

---

## 1. 对外发布物自检（distribution gate）

- 命令：`python scripts/verify_distribution.py --json`
- 结论：**PASS（5/5 通过）**
- 策略：`min_overall_score=80`，`block_severities=[critical, high]`，`require_source_in_repo=true`
- 不变量（与主扫描器一致）：`no_execute=true`、`no_network=true` —— 仅读取发布源，**绝不执行其中的任何命令**，不发起网络请求。

| 产物 | 渠道 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| chinese-seo-compliance | agensi | in_repo | 2 | 97 | 0 | 通过 |
| aishield-security-scan | claude-skills | in_repo | 1 | 96 | 0 | 通过 |
| aishield-gpt | gpt-store | in_repo | 1 | 95 | 0 | 通过 |
| aishield-agent-security-benchmark | huggingface | in_repo | 1 | 94 | 0 | 通过 |
| aishield | clawhub | in_repo | 1 | 94 | 0 | 通过 |

- 命中统计：5 个产物共 12 条非阻断发现，0 条 critical/high 阻断项。
- 源留底：全部 `in_repo`，无 `missing` 项（无「已发布但仓库无法复核」的硬伤）。
- **铁律核验**：对外发布物均过自家扫描器，本日无高危未修项，未静默跳过。

---

## 2. 线上存活（health gate）

### 2a. 远端 `data/state/health.json`（GitHub Contents API, ref=main）

- 读取方式：`curl --ssl-no-revoke --tlsv1.3 GET api.github.com/.../contents/data/state/health.json`（**非本地副本**）
- `updated`：**2026-08-31T21:31:35.292736+00:00**（≈ 本运行前 3 小时，新鲜）
- blob sha：`ff17f1a32243`
- 结论：**PASS**（远端副本近期被刷新，非陈尸）

### 2b. `aishield.tools` 首页

- `curl -sS --ssl-no-revoke --tlsv1.3 GET https://aishield.tools/`
- HTTP **200**，28,509 bytes，有效 HTML（`<!DOCTYPE html>`、`lang="zh-CN"`）
- 延迟 ≈ 0.98s
- 结论：**PASS**（站点存活）

> 注意：本地 `data/state/health.json` 的 `updated` 为 2026-08-05（陈旧副本），不以之作判断依据，本日以远端为准。

---

## 3. 测试套件（`tests/run_all.py`）

- 命令：`python tests/run_all.py`
- 结论：**PASS**
- 汇总：**Total 590 / Passed 590 / Failed 0 / Errors 0 / Skipped 12**
- 基准对比：基准 523/0/9 → 现 590/0/12（用例增长，仍全绿，0 失败）
- 核心契约（良性零误报 + 恶意拦截组）随套件全绿通过；`test_security` / `test_mcp_contract` 等契约模块均 OK。

---

## 总评

| 检查项 | 结论 | 关键异常 |
|--------|------|----------|
| 1. 对外发布物自检 | PASS | 无 |
| 2. 线上存活 | PASS | 无 |
| 3. 测试套件 | PASS | 无 |

三项全部 PASS，**无关键异常**。按「反馈集中化（2026-08-23 起）」规则：本日为常规日，不单独推送聊天告警，仅写入 `.workbuddy/memory/automation-digest/2026-09-01.md`，由周日 20:00 合并周报统一汇报。

> 约束自检：本运行未登录任何外部平台，未替用户发布或发邮件；仅读取与写入仓库内报告/状态文件。
