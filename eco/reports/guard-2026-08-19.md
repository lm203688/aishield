# AIShield 安全守夜报告 · 2026-08-19

> 执行时间：2026-08-19 08:30 (GMT+8) · 自动化 `automation-1786840209961`
> 本机 TLS 代理仅放行 TLS1.3，所有远端探测均带 `--ssl-no-revoke --tlsv1.3`。

## 总览

| 项 | 状态 | 结论 |
|----|------|------|
| ① 对外发布物自检门禁 | 🟢 通过 | 5/5 通过，0 阻断 |
| ② 线上存活 | 🟢 存活 | health 探针 ~5 分钟前刷新（score 4/4, http 200）；aishield.tools 本机探测超时（本地代理出口未放行，非全球宕机） |
| ③ 测试套件 | 🟢 绿 | 550 通过 / 0 失败 / 9 跳过 |

**无 🔴 阻断异常。** 三项全部通过，无高危未修项、无测试转红、无线上失活。

---

## ① 对外发布物自检门禁（`scripts/verify_distribution.py`）

> 铁律：对外发布物必须过自家扫描器。复用 `scanner.workspace_scan._local_pipeline`，只读扫描、不执行任何命令、不发起网络请求。
> 策略：最低分 80，阻断等级 critical/high，要求源留底。

| 渠道 | 产物 | 源版本 | 线上版本 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|----------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | 1.1.0 | 1.0.0 | rebuilt | 2 | 97 | 0 | ✅ PASS |
| claude-skills | aishield-security-scan | 4.2.2 | — | in_repo | 1 | 96 | 0 | ✅ PASS |
| gpt-store | aishield-gpt | 4.2.2 | — | in_repo | 1 | 95 | 0 | ✅ PASS |
| huggingface | aishield-agent-security-benchmark | 4.2.2 | — | in_repo | 1 | 94 | 0 | ✅ PASS |
| clawhub | aishield | 4.2.2 | — | in_repo | 1 | 94 | 0 | ✅ PASS |

**结论：5/5 通过，0 失败，0 阻断级发现。** 评分区间 94–97，全部远高于阈值 80。

⚠️ **非阻断提示（持续自 08-17）**：`agensi/chinese-seo-compliance` 仓库源版本已升到 **1.1.0**，但线上仍发布 **1.0.0**，二者不一致。下次更新 Agensi 时需覆盖发布 1.1.0 以同步。不影响安全门禁，仅作版本对账提醒。

---

## ② 线上存活（curl + GitHub Contents API）

**远端 `data/state/health.json`**（经 Contents API 读取，非本地副本）：

| 字段 | 值 |
|------|-----|
| `updated` | 2026-08-19T00:25:12.672551+00:00（≈北京时间 08:25） |
| 距本次守夜 | ~5 分钟（存活） |
| `current.healthy` | true（level=healthy） |
| `current.score` | 4 / 4 |
| `current.http` | 200 |
| `consecutive_failures` | 0 |

**`aishield.tools` 首页**：本机探测 `HTTP=000`，curl 报错 `Failed to connect to aishield.tools:443 after 21548 ms`（exit 28）。
⚠️ **判定：本地代理出口未放行该域名，非全球宕机。** 旁证为上方远端健康探针 `current.http=200`、score 满分 4/4、连续失败 0 —— 说明站点在全球视角正常可达。该 000 现象与 08-17 一致，属本机守夜主机代理出口限制；建议用户在浏览器侧直接访问 `https://aishield.tools/` 核验（或经其他网络出口复测）。

**结论：线上存活正常。** 健康探针在守夜窗口内（~5 分钟前）刷新，score 满分，连续失败 0。

---

## ③ 测试套件（`python tests/run_all.py`）

```
Total:    550
Passed:   550
Failed:   0
Errors:   0
Skipped:  9
```

- 较查询基准 523 增加 27 例（08-18 为 536 → 08-19 为 550，新增测试模块已并入主套件）。
- **失败数恒为 0**，核心契约（良性零误报 + 恶意仍拦 40 项）随套件一并通过。
- 跳过 9 项为非阻断环境跳过，与历史一致。

---

## 备注与提醒

1. **agensi 版本漂移（非阻断）**：源 1.1.0 / 线上 1.0.0，待下次覆盖发布。
2. **aishield.tools 本机探测超时（非阻断）**：本机守夜主机代理出口未放行 aishield.tools:443（curl 28 超时），但远端健康探针确认 http 200 / score 4/4，站点实际存活。建议用户在浏览器或其他出口核验。
3. **aishield.tools 站源码漂移未在本次门禁范围**：本守夜只校验发布源（distribution/）与测试/线上存活，不重扫 `api/static/` 线上源码。线上站 CF Pages 重建仍依赖用户在 Cloudflare dashboard 手动 Retry（cfut_ token 权限不足，本地无法 API 重建）——属已知用户侧动作，非本次异常。
4. 未登录任何外部平台、未替用户发布或发邮件。本运行仅生成报告并推送。

---
*不变量核验：自带洞必检 ✅ ｜ 持续鉴证 ✅ ｜ 健康门禁 ✅*
