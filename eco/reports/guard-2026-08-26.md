# AIShield 每日安全守夜报告 — 2026-08-26

> 运行时刻：2026-08-26 08:30 (北京时间) ｜ 自动化 `automation-1786840209961`
> 结论：**三项全绿，无 🔴 阻断异常（常规运行，仅写 digest，由周日 20:00 周报合并汇报）**

## ① 对外发布物安全自检门禁
- 命令：`python scripts/verify_distribution.py`
- 退出码：**0** ｜ 通过 **5/5** ｜ 阻断项 **0**
- 评分区间 **94–97**（clawhub 94 / huggingface 94 / gpt-store 95 / claude-skills 96 / agensi 97）
- 不变量已遵守：只读扫描，不执行发布物中任何命令、不发起网络请求。
- ⚠️ 持续非阻断提示（自 08-17 起第 **10** 天）：`agensi/chinese-seo-compliance` 线上版本 **1.0.0** 与仓库源版本 **1.1.0** 不一致，下次更新需覆盖发布。**非安全洞，不阻断。**

| 渠道 | 产物 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | rebuilt | 2 | 97 | 0 | 通过 |
| claude-skills | aishield-security-scan | in_repo | 1 | 96 | 0 | 通过 |
| gpt-store | aishield-gpt | in_repo | 1 | 95 | 0 | 通过 |
| huggingface | aishield-agent-security-benchmark | in_repo | 1 | 94 | 0 | 通过 |
| clawhub | aishield | in_repo | 1 | 94 | 0 | 通过 |

## ② 线上存活探测（curl 带 `--ssl-no-revoke --tlsv1.3`）
- **远端 `data/state/health.json`**（经 GitHub Contents API 直读，非本地副本）
  - blob SHA `4ad181e9f6140694d42fa10bfc152d3ac752ea59`
  - `current.checked_at = 2026-08-25T23:27:48Z`（≈北京时间 08-26 07:27，**约 1h03m 前**，在探针小时级节奏内）
  - `healthy = true` ｜ `score = 4/4` ｜ `http = 200` ｜ `consecutive_failures = 0` ｜ `latency ≈ 0.385s`
- **aishield.tools 首页**：`HTTP = 200`（curl 退出码 23 为写 /dev/null 的本地 TLS 代理怪癖，不影响判定，首部 HTTP 码权威）。
- 健康 schema 字段沿用 `current.checked_at`（文档/代码漂移，非异常）。

## ③ 测试套件（核心契约门禁）
- 命令：`python tests/run_all.py`
- **572 通过 / 0 失败 / 0 错误 / 12 跳过**（较 08-25 的 557 再 +15 新用例，失败数恒 0）
- 核心契约保留：**良性样本零误报 + 恶意样本仍拦截 40 项**。
- 全部 25/25 测试模块加载，`ALL TESTS PASSED`。

## 运维备注
- 未登录任何外部平台、未替用户发布或发邮件。
- 仅本守夜报告为新增产物；经 `scripts/gh_push.py` 推 main 并用 Contents API 复验。
- 长期观察项：agensi 线上/源版本不一致已持续 10 天，建议择机覆盖发布。
