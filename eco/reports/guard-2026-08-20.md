# AIShield 每日安全守夜报告 — 2026-08-20

- **运行时刻**：2026-08-20 08:30 (北京时间)
- **总判定**：✅ 无 🔴 产品级阻断；⚠️ 发现 1 处本地同步滞后（门禁一度转红，已即时修复并复跑全绿）
- **对外发布物门禁**：通过 (5/5, 评分 94–97, 0 阻断)
- **线上存活**：✅ 健康 (远端探针 4/4, http 200) + aishield.tools 首页 HTTP 200
- **测试门禁**：首次运行 🔴 6 errors → 根因定位为本地工作树缺失夹具 → 已从远端恢复 → **557 通过 / 0 失败 / 0 错误 / 9 跳过（全绿）**

---

## ⚠️ 顶部提醒（真实异常，已处理）

**守夜门禁今早首次捕获测试转红**，但经根因分析为 **本地工作树与远端 main 不同步**，并非产品缺陷：

- 首次跑 `tests/run_all.py` → **551 通过 / 0 失败 / 6 errors / 9 跳过**，6 个 error 全部来自 `tests/test_geo.py::TestAgentCardJson` 的 `setUp`：`FileNotFoundError: api/static/.well-known/agent-card.json`。
- 实地核验：该文件在 **远端 main 存在**（GitHub Contents API `api/static/.well-known/agent-card.json` → HTTP 200），`tests/test_geo.py` 也在远端（HTTP 200）。即远端 CI 应通过，**本地 checkout 缺该夹具**。
- 本机 `.git` 长期损坏（无 commit 历史，分支 `release-workflows` 无提交），无法走 `git pull` 同步，故工作树滞后于远端。
- **已处置**：从远端权威副本恢复本地 `api/static/.well-known/agent-card.json`（15848 B，校验 name=AIShield / version=4.2.2 / 含 skills + trust_score），复跑测试 → **全绿 557/0/0/9**。
- 此修复仅同步本地、不改产品，故**不推送该文件**（远端已存在）。

> 给用户的运维提示：本机工作树长期未与远端同步（.git 损坏），守夜 agent 依赖的本地副本可能持续滞后。若需长期稳定，建议修复本地 git 或改用 gh_push/Contents API 拉取基线。

---

## ① 对外发布物自检门禁（`scripts/verify_distribution.py`）

**结论：全部通过（共 5 条），退出码 0。**

| 渠道 | 产物 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | rebuilt | 2 | 97 | 0 | 通过 |
| claude-skills | aishield-security-scan | in_repo | 1 | 96 | 0 | 通过 |
| gpt-store | aishield-gpt | in_repo | 1 | 95 | 0 | 通过 |
| huggingface | aishield-agent-security-benchmark | in_repo | 1 | 94 | 0 | 通过 |
| clawhub | aishield | in_repo | 1 | 94 | 0 | 通过 |

**持续非阻断提示**（自 08-17 起，非阻断）：agensi 线上版本 1.0.0 与仓库源版本 1.1.0 不一致，下次更新需覆盖发布。

> 不变量：只读扫描，不执行发布物中的任何命令，不发起网络请求。

---

## ② 线上存活核验（curl 带 `--ssl-no-revoke --tlsv1.3`）

**a. 远端 `data/state/health.json`（GitHub Contents API 读取，本地副本不算数）**

- `updated` = **2026-08-20T00:25:54Z**（≈ 北京时间 08:25，距守夜约 5 分钟，存活）
- `healthy` = true，`level` = healthy，`score` = **4 / 4**，`http` = 200，`latency` ≈ 0.584s，`consecutive_failures` = 0
- 历史连续 2026-08-18 23:26 → 2026-08-20 00:25 多路探测全部 healthy，无失败记录。

**b. aishield.tools 首页**

- HTTP = **200**（curl 退出码 23 为写 `/dev/null` 的本地代理小怪癖，不影响状态判定）。

✅ 线上站点与远程健康探针双路存活确认。

---

## ③ 测试门禁（`tests/run_all.py`）

| 阶段 | 结果 |
|------|------|
| 首次运行 | 551 通过 / 0 失败 / **6 errors** / 9 跳过（🔴 门禁红） |
| 根因 | 本地缺 `api/static/.well-known/agent-card.json` 测试夹具（远端 main 存在，本地同步滞后） |
| 处置 | 从远端恢复夹具，复跑 |
| **最终** | **557 通过 / 0 失败 / 0 错误 / 9 跳过（✅ 全绿）** |

- **核心契约保留**：良性零误报（`test_sandbox_rules::test_benign_zero_fp`、tech_radar 良性语料误报拦截等）+ 恶意仍拦（`test_sandbox_rules::test_each_malicious_hits`、`test_agent_security_gateway` 四类威胁拦截等）均在 557 全绿中包含通过。
- 较基准 523 多出的用例为既有新增（含 `test_geo` 的 Agent Card 6 项），失败数恒 0。

---

## 附录：执行命令与复验

```bash
# ① 发布物门禁
python scripts/verify_distribution.py        # EXIT=0, 5/5 PASS

# ② 线上存活
curl -sS --ssl-no-revoke --tlsv1.3 \
  https://api.github.com/repos/lm203688/aishield/contents/data/state/health.json   # updated=2026-08-20T00:25:54Z, score 4/4
curl -sS --ssl-no-revoke --tlsv1.3 -o /dev/null -w "HTTP=%{http_code}" \
  https://aishield.tools/                                                     # HTTP=200

# ③ 测试（修复本地夹具后）
python tests/run_all.py                       # 557 passed / 0 failed / 0 errors / 9 skipped
```

- 本报告以 `scripts/gh_push.py` 推送至 `lm203688/aishield@main`，并以 Contents API 复验。
- 本次**未登录任何外部平台、未替用户发布或发邮件**。
- 本地恢复的文件 `api/static/.well-known/agent-card.json` 与远端一致，不重复推送。
