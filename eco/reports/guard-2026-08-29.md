# AIShield 安全守夜报告 · 2026-08-29 (08:30 北京时间)

> 自动生成 · 三项门禁：发布物自检 / 线上存活 / 测试契约

## 总览

| 检查项 | 结论 | 关键指标 |
|--------|------|----------|
| ① 对外发布物自检（distribution gate） | ✅ PASS | 5/5 通过，评分 94–97，0 阻断项 |
| ② 线上存活（远程健康 + 首页） | ✅ PASS | 探针 checked_at 2026-08-28T23:56:42Z，healthy；aishield.tools HTTP 200 |
| ③ 测试契约（tests/run_all.py） | ✅ PASS | 590 通过 / 0 失败 / 0 错误 / 12 跳过 |

**无 🔴 关键异常。** 本机未发现测试转红、发布物带洞、线上失活。

---

## ① 对外发布物自检（verify_distribution.py）

退出码 `0`，策略：最低分 80、阻断等级 critical/high、要求源留底。

| 渠道 | 产物 | 源状态 | 文件数 | 评分 | 阻断项 | 结果 |
|------|------|--------|--------|------|--------|------|
| agensi | chinese-seo-compliance | rebuilt | 2 | 97 | 0 | 通过 |
| claude-skills | aishield-security-scan | in_repo | 1 | 96 | 0 | 通过 |
| gpt-store | aishield-gpt | in_repo | 1 | 95 | 0 | 通过 |
| huggingface | aishield-agent-security-benchmark | in_repo | 1 | 94 | 0 | 通过 |
| clawhub | aishield | in_repo | 1 | 94 | 0 | 通过 |

**非阻断提示（持续）**：agensi 线上版本 1.0.0 与仓库源版本 1.1.0 不一致，下次更新需覆盖发布（自 2026-08-17 起持续第 13 天，非安全洞）。

不变量已遵守：只读扫描，不执行发布物中的任何命令，不发起网络请求。

## ② 线上存活

**远程健康（GitHub Contents API 直读，非本地副本）**
- `data/state/health.json` 经 Contents API 读到 `current.checked_at = 2026-08-28T23:56:42Z`（北京时间 08-29 07:56，距守夜约 34 分钟，在探针小时级节奏内）。
- `healthy = true`，`level = healthy`，`score = 4/4`，`http = 200`，`consecutive_failures = 0`，`latency = 0.487s`。

**首页探测**
- `https://aishield.tools/` → `HTTP=200`（curl 退出码 23 为写 `/dev/null` 的本地代理怪癖，不影响 `-w` 捕获的 HTTP_CODE 判定）。

结论：站点全球存活、探针持续回写，无失活。

## ③ 测试契约（tests/run_all.py）

```
Total:    590
Passed:   590
Failed:   0
Errors:   0
Skipped:  12
============================================================
ALL TESTS PASSED
```

- 较 08-28（590）持平，失败/错误恒 0。
- **核心契约保持**：良性样本零误报 + 恶意样本仍拦 40 项（test_ci_contract 等门禁用例持续通过）。

---

## 运维备注

- 未登录任何外部平台、未替用户发布或发邮件。
- 本机 TLS 代理只放行 TLS1.3，所有 curl 均带 `--ssl-no-revoke --tlsv1.3`。
- 本报告由守夜自动化生成并经 `scripts/gh_push.py` 推送 main，`gh_push` 推送后做 Contents API 复验。
