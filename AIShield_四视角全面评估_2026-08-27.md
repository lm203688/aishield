# AIShield 四视角全面评估（2026-08-27）

> 视角：终端用户 / 技术专家 / 投资人 / 领域专家。所有数据基于代码实地核查（不是记忆推断）。
> 基线：v4.2.2, 153 Python 文件, 26 eco 模块 + 38 scanner 模块 + 29 测试, 572/572 测试绿, 零第三方依赖。

---

## 0. 数据快照（从代码核查出来的硬事实）

| 维度 | 实测值 |
|------|--------|
| Python 模块总数 | 153 |
| 生态模块 (eco/) | 26 |
| 扫描器模块 (scanner/) | 38 |
| 测试文件 | 29 |
| 测试用例 | 572 passed / 0 failed / 12 skipped |
| MCP 工具 | 14（其中 9 个 `aishield_*` 新命名 + **5 个旧名并存**：`security_scan`/`prompt_injection_check`/`banned_words_check`/`rug_pull_detect`/`dependency_monitor`） |
| 规则数 | MCP 227 + Skill 233 |
| 第三方依赖 | 0 |
| 版本一致性 | **未过** — `api/server.py:405 serverInfo` 仍报 4.2.0；`api/openapi_spec.py:67,135` 仍报 4.2.0（仅 `setup.py`/`pyproject.toml`/`mcp-server/*` 过 7 点门禁） |
| 线上状态 | Cloudflare Pages 陈旧 4.2.0/133/错工具名（cfut_ token 权限不足，无法 API 重建） |
| 已上架 | npm 4.2.2 · Glama · Official MCP Registry（4.2.2 active） |

---

## 1. 四视角评估

### 1.1 终端用户视角（开发者 / 企业安全负责人）

**做得好的**：
- 真本地优先，零网络、零上传。安全敏感客户最核心的信任锚。
- MCP JSON / Skill 目录 / GPTs.json / prompt.md 全格式输入，扫一次覆盖所有 agent 面。
- CLI + REST + MCP 三种接入方式，覆盖不同技术栈。
- 中文支持（6 大平台违禁词、中文 prompt 注入规则），本地开发者友好。

**硬伤（必须指出）**：
1. **入门门槛偏高**：装 Python + 配 MCP JSON + CLI。没有 VS Code 插件、没有桌面 GUI，企业用户"装好扫一次"的闭环还没做。
2. **规则不可浏览**：227+233 规则是代码级的，普通安全负责人看不到"扫到了什么、为什么命中、怎么修"。缺一个可视化规则浏览器。
3. **旧工具名并存**：`security_scan` 与 `aishield_scan` 两个工具都做同一件事，用户不知道用哪个。这是线上 4.2.0 漂移的**代码层面残留**，也是用户困惑的直接来源。
4. **版本漂移是致命伤**：`aishield.tools` 显示 4.2.0 + 133 规则 + 错工具名，实际 main 是 4.2.2 + 227/233 + 新工具名。**用户看到的东西 ≠ 你交付的东西**，这是信任崩塌的起点。
5. **dashboard.html 是静态页**：读本地 blackboard/trust 数据，需要用户先跑过其他流程才有内容。冷启动用户体验为零。

### 1.2 技术专家视角

**做得好的**：
- 零依赖设计哲学一致到底，`urllib` 兜底所有网络。
- 分层清晰：scanner（规则 + 引擎 + SBOM + OSV）/ eco（身份 + 支付 + 协作 + 治理）/ api（MCP + 生态路由）。
- Ed25519 `did:key` + TrustRegistry 真实现，不是 spec 空壳。
- 11 个 OWASP Agentic 模块覆盖完整，attack_replay / vertical_risk / guardrail_harness 是 2025-2026 前沿方向。
- 测试 572 例绿，含 8 个场景 demo（含安全闸/鉴证漂移/任务锁）。

**硬伤**：
1. **版本漂移三处**：`server.py:405`（serverInfo）、`openapi_spec.py:67,135`。7 点门禁没覆盖到，是 CI 门控的漏网。**必须修**。
2. **命名冲突 5 对**：新旧工具名并存，是 429 中断时未清理的历史残留。用户 `tools/list` 看到 14 个工具，9 个是新命名、5 个是旧命名重复。必须合并或 deprecate。
3. **没有真 LLM 后端接入**：scanner 是纯规则 + 正则，缺语义级检测。本地 Ollama 端点（127.0.0.1:11434）有配置但未接到语义扫描链路（如攻击路径解释、暗模式判定）。这是**能力天花板**。
4. **无差分扫描**：增量扫描（对比上次 commit 只扫变化部分）没有，大仓库扫一次很慢。
5. **规则手写维护成本高**：227+233 条全靠人写，缺 arXiv/CVE 自动转规则 pipeline（虽然 tech_radar 有候选区，但没闭环到规则落盘）。
6. **SBOM 只生成不消费**：`scanner/sbom.py` 生成 CycloneDX，但没有消费方（没有接 x402/USDC 结算）。

### 1.3 投资人视角

**卡位价值**：
- 空白市场：本地优先 + MCP+Agentic 双维 + 中立信任机构，三个标签在竞争图谱里同时打的只有 AIShield。
- 与 Cloudflare 生态位互补（CF Sandbox 做 OS 隔离，AIShield 做内容信任平面），不冲突。
- 零成本运维，毛利结构理论上极好。
- 社区化规则体系（227/233）是数据护城河。

**风险**：
1. **商业化路径未验证**：x402/USDC 是 spec，`hupijiao.py` 有实现但未接入真支付（license.swarmlabs.tools 是另一个项目）。AIShield 自己没收入流。
2. **无标杆客户**：公开案例为零。安全产品没 case study 很难卖。
3. **竞争窗口窄**：mcp-audit（89 SAST、全离线）和 Sunglasses（100% 本地）是真正竞品，且都是"扫 MCP 配置"的子集。AIShield 的领先优势是**可见性**（GEO/agent 化露出）不是能力。
4. **线上漂移是商业信号**：官网陈旧 4.2.0，投资人打开第一眼看到的就是"这项目是否在认真运营"的答案。
5. **Agent-First 是故事不是产品**：`agent_gateway.py` 注册/quick_scan 有实现，但没有真实 agent 流量接入验证。

### 1.4 领域专家视角（OWASP MCP SIG / Agent 安全社区）

**亮点**：
- 对齐 OWASP MCP Top10 + Agentic ASI01–10，规则覆盖完整。
- `eco/trust_protocol.py` 是独立可复用的可信元数据层，可作为跨项目资产。
- `agent-card.json` AIP 字段（`aip_protocol_version`/`AIP-AC/v1`/`attack_replay`/`vertical_risk_scan`）对齐 agent-native discovery，是行业首创。
- `llms.txt` Whisper 旁审计 + BYOA 内容信任层叙事，GEO 叙事正确。

**不足**：
1. **未参与 OWASP MCP SIG 贡献**：没有 upstream 贡献记录，行业话语权弱。
2. **无第三方测评/独立案例**：安全产品没有第三方背书很难进入企业采购清单。
3. **Nucleus/SIEM 适配器未完成**（F3 挂起）：企业安全运营闭环缺关键一环。
4. **SBOM + attestation 未闭环**：生成 CycloneDX 但没有消费方，也没有 Ed25519 attestation 签名的真流转。
5. **无 fuzzing / 攻击自动化**：有 attack_replay（重放），但没有 fuzzing（发现新攻击面）。

---

## 2. 四维度审核评分

| 维度 | 评分 | 关键扣分项 |
|------|------|------------|
| **完整性** | 7.5/10 | 规则体系全、11 模块全；但 Nucleus/SIEM 适配器缺、LLM 语义后端缺、差分扫描缺 |
| **可用性** | 6/10 | 版本漂移 4.2.0、命名冲突 5 对、冷启动 dashboard 空白、无 GUI/VS Code 插件、入门门槛高 |
| **先进性** | 8/10 | OWASP Agentic 对齐 + Ed25519 did:key + AIP agent-card + x402 spec；但无 fuzzing、无真 LLM 语义扫描 |
| **安全性** | 8.5/10 | 零依赖 + 本地优先 + Ed25519 + 真 sandbox（agent_security_gateway）；但版本漂移暴露了发布流程缺陷 |

**综合：7.4/10**。技术底座扎实，**可用性是最大的短板**，且直接体现在线上漂移和命名冲突两个用户最敏感的点上。

---

## 3. 自动化运行迭代策略

### 3.1 优先级铁律（不做 = 白做）

1. **可用性 > 先进性**：线上漂移和命名冲突必须先修，否则后面所有先进性都是"藏在代码里的"，用户看不见。
2. **可结算 > 可宣传**：x402/hupijiao 要落到真交易闭环，不是 spec 展示。
3. **数据飞轮 > 单点功能**：规则自动从 arXiv/CVE 转录到落盘是最高杠杆。

### 3.2 自动化闭环四件套

| 闭环 | 现状 | 目标 | 杠杆 |
|------|------|------|------|
| **规则飞轮** | tech_radar 有候选区，无落盘闭环 | arXiv→候选→测试→规则落盘→自动发版 | 最高（每条规则 = 一个安全事件覆盖） |
| **发布飞轮** | sync_version.py 7 点门禁漏 server.py:405 + openapi_spec.py | 10 点门禁 + CF Pages 自动重建 + GitHub Pages 停维护 | 直接修 4.2.0 漂移 |
| **测试飞轮** | 572/572 本地绿，无远端 CI 绿 | push 后自动跑远端 CI（GitHub Actions 若 PAT 有 workflow 权限；否则 webhook→ECS） | 防止推了坏的 |
| **告警飞轮** | 4 个定时任务已集中 digest + 周日周报 | digest 自动生成周报 → 推 user 邮箱（agent-mail） | 用户"低摩擦决策" |

### 3.3 自动化改造清单（可直接落地的 5 件事）

1. **修 `sync_version.py` 门禁**：把 `api/server.py` 的 serverInfo version + `api/openapi_spec.py` 的 version 纳入 7 点 → 变成 9 点门禁。改完跑 `--check` 强制失败，逼着修。
2. **清理旧工具名**：5 个旧工具（`security_scan` 等）在 `tools/list` 里 deprecate 标注（加 `deprecated: true` 字段），或者直接删。用户只该看到 9 个干净工具。
3. **CF Pages 重建自动化**：cfut_ token 权限不足是硬阻塞，唯一解是用户去 CF dashboard Retry。**但可以做降级**：把 `docs/` 部署到 npm `files` 目录，下次 npm publish 自动带最新 `server-card.json`，绕过 CF Pages。
4. **规则自动转录**：写一个 `scripts/arxiv_to_rule.py`：抓 arXiv 新安全论文 → 抽关键词 → 对映到 227/233 规则 → 若新增模式 → 生成 `_proposed/` stub + 单元测试。这是 tech_radar signal→candidate→promote→effect 的最后一环。
5. **dashboard 冷启动修复**：`dashboard.html` 在没有 blackboard 数据时显示"跑一次 `aishield_scan` 生成首份报告"的引导，而不是空表。

---

## 4. 技术建议（按投入产出排序）

| 序号 | 建议 | 投入 | 产出 | 优先级 |
|------|------|------|------|--------|
| 1 | 修 9 点版本门禁 + 清理 5 个旧工具名 | 0.5 天 | 消除线上漂移和命名混乱 | **P0** |
| 2 | 接入本地 Ollama 做语义级攻击路径解释 | 1-2 天 | 把规则匹配 → 语义判定，能力天花板提升 | **P0** |
| 3 | arXiv→规则自动转录 pipeline | 1 天 | 数据飞轮闭环，每月自动新增规则 | **P1** |
| 4 | 差分扫描（对比上次 commit 只扫变动的文件） | 1 天 | 大仓库扫描时间从分钟级降到秒级 | **P1** |
| 5 | Nucleus/SIEM 适配器（F3） | 2 天 | 企业安全运营闭环 | **P1** |
| 6 | x402/hupijiao 真交易闭环（license 售卖） | 2 天 | 商业化从 spec → 收入 | **P2** |
| 7 | VS Code 扩展 | 3 天 | 降低入门门槛 | **P2** |
| 8 | fuzzing 模块 | 3-5 天 | 发现新攻击面（不仅是重放旧攻击） | **P2** |
| 9 | 第三方案例/测评（找 1-2 个开源项目公开跑分） | 3 天 | 投资人/企业采购的 case study | **P2** |
| 10 | OWASP MCP SIG upstream 贡献 | 持续 | 行业话语权 | **P3** |

---

## 5. 一句话结论

**技术底座是 8.5 分，可用性是 6 分，差距是 2.5 分——这 2.5 分全部卡在"用户第一眼看到的线上漂移和命名混乱"上。** 修好这两件事，AIShield 从"好代码藏起来"变成"好产品摆出来"，投资人和用户看到的才是真实的 8.5 分。

自动化迭代的唯一方向：**让规则飞轮转起来，让发布飞轮干净，让测试飞轮远端绿，让告警飞轮替用户决策。** 四个飞轮转起来，AIShield 才从"扫描器"变成"平台"。
